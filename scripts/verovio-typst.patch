diff --git a/include/hum/humlib.h b/include/hum/humlib.h
index f65c51831..931b8cf2b 100644
--- a/include/hum/humlib.h
+++ b/include/hum/humlib.h
@@ -691,7 +691,6 @@ class HumRegex {
 		std::regex_constants::match_flag_type
 				getTemporarySearchFlags(const std::string& sflags);
 
-
 	private:
 
 		// m_regex: stores the regular expression to use as a default.
@@ -5640,7 +5639,7 @@ int main(int argc, char** argv) {                      \
 	if (interface.getArgCount() > 0) {                  \
 		ifstream input(interface.getArgument(1));        \
 		if (!input) {                                    \
-			throw std::runtime_error("Could not open file: " + interface.getArgument(1)); \
+			return -1; \
 		}                                                \
 		stringstream buf;                                \
 		buf << input.rdbuf();                            \
diff --git a/include/tuning-library/TuningsImpl.h b/include/tuning-library/TuningsImpl.h
index d405db685..1afd3f7d7 100644
--- a/include/tuning-library/TuningsImpl.h
+++ b/include/tuning-library/TuningsImpl.h
@@ -113,11 +113,7 @@ inline Tone toneFromString(const std::string &fullLine, int lineno)
 
         if (t.ratio_n == 0 || t.ratio_d == 0)
         {
-            std::string s = "Invalid tone in SCL file.";
-            if (lineno >= 0)
-                s += "Line " + std::to_string(lineno) + ".";
-            s += " Line is '" + line + "'.";
-            throw TuningError(s);
+            return t;
         }
         // 2^(cents/1200) = n/d
         // cents = 1200 * log(n/d) / log(2)
@@ -186,7 +182,7 @@ inline Scale readSCLStream(std::istream &inf)
             res.count = atoi(line.c_str());
             if (res.count < 1)
             {
-                throw TuningError("Invalid SCL note count.");
+                return res;
             }
             state = read_note;
             break;
@@ -217,15 +213,12 @@ inline Scale readSCLStream(std::istream &inf)
             oss << "unknown state.";
             break;
         }
-        throw TuningError(oss.str());
+        return res;
     }
 
     if ((int)res.tones.size() != res.count)
     {
-        std::string s =
-            "Read fewer notes than count in file. Count = " + std::to_string(res.count) +
-            " notes. Array size = " + std::to_string(res.tones.size());
-        throw TuningError(s);
+        return res;
     }
     res.rawText = rawOSS.str();
     return res;
@@ -260,7 +253,7 @@ inline Scale readSCLFile(const StreamablePath auto &path)
         }
 
         errMsg += "'";
-        throw TuningError(errMsg);
+        return {};
     }
 
     auto res = readSCLStream(inf);
@@ -324,11 +317,9 @@ inline Scale evenTemperament12NoteScale()
 inline Scale evenDivisionOfSpanByM(int Span, int M)
 {
     if (Span <= 0)
-        throw Tunings::TuningError("Span should be a positive number. You entered " +
-                                   std::to_string(Span));
+        return {};
     if (M <= 0)
-        throw Tunings::TuningError(
-            "You must divide the period into at least one step. You entered " + std::to_string(M));
+        return {};
 
     std::ostringstream oss;
     oss.imbue(std::locale("C"));
@@ -349,11 +340,9 @@ inline Scale evenDivisionOfSpanByM(int Span, int M)
 inline Scale evenDivisionOfCentsByM(float Cents, int M, const std::string &lastLabel)
 {
     if (Cents <= 0)
-        throw Tunings::TuningError("Cents should be a positive number. You entered " +
-                                   std::to_string(Cents));
+        return {};
     if (M <= 0)
-        throw Tunings::TuningError(
-            "You must divide the period into at least one step. You entered " + std::to_string(M));
+        return {};
 
     std::ostringstream oss;
     oss.imbue(std::locale("C"));
@@ -425,9 +414,7 @@ inline KeyboardMapping readKBMStream(std::istream &inf)
             }
             if (!validLine)
             {
-                throw TuningError("Invalid line " + std::to_string(lineno) + ". line='" + line +
-                                  "'. Bad character is '" + badChar + "/" +
-                                  std::to_string((int)badChar) + "'");
+                return res;
             }
         }
 
@@ -503,14 +490,12 @@ inline KeyboardMapping readKBMStream(std::istream &inf)
             oss << "unknown state";
             break;
         }
-        throw TuningError(oss.str());
+        return res;
     }
 
     if ((int)res.keys.size() != res.count)
     {
-        throw TuningError("Different number of keys than mapping file indicates. Count is " +
-                          std::to_string(res.count) + " and we parsed " +
-                          std::to_string(res.keys.size()) + " keys.");
+        return res;
     }
 
     res.rawText = rawOSS.str();
@@ -552,7 +537,7 @@ inline KeyboardMapping readKBMFile(const StreamablePath auto &path)
         }
 
         errMsg += "'";
-        throw TuningError(errMsg);
+        return {};
     }
 
     auto res = readKBMStream(inf);
@@ -610,8 +595,7 @@ inline Tuning::Tuning(const Scale &s_, const KeyboardMapping &k_, bool allowTuni
     KeyboardMapping k = k_;
     int oSP = 1;
     if (s.count <= 0)
-        throw TuningError("Unable to tune to a scale with no notes. Your scale provided " +
-                          std::to_string(s.count) + " notes.");
+        return;
 
     int useMiddleNote{k.middleNote};
     if (k.count > 0)
@@ -674,8 +658,7 @@ inline Tuning::Tuning(const Scale &s_, const KeyboardMapping &k_, bool allowTuni
     // smaller than the size of the scale.
     if (k.octaveDegrees > s.count)
     {
-        throw TuningError("Unable to apply mapping of size " + std::to_string(k.octaveDegrees) +
-                          " to smaller scale of size " + std::to_string(s.count));
+        return;
     }
 
     int posPitch0 = 256 + k.tuningConstantNote;
@@ -704,7 +687,7 @@ inline Tuning::Tuning(const Scale &s_, const KeyboardMapping &k_, bool allowTuni
                  std::to_string(oSP) + " given your scale root of " + std::to_string(k.middleNote) +
                  " which your mapping does not assign. Please set your tuning constant "
                  "note to a mapped key.";
-            throw TuningError(s);
+            return;
         }
     }
     double tuningCenterPitchOffset;
@@ -808,7 +791,7 @@ inline Tuning::Tuning(const Scale &s_, const KeyboardMapping &k_, bool allowTuni
                          std::to_string(k.middleNote) +
                          " which your mapping does not assign. Please set your tuning constant "
                          "note to a mapped key.";
-                    throw TuningError(s);
+                    return;
                 }
             }
             scalepositiontable[i] = scalePositionOfTuningNote % s.count;
@@ -880,9 +863,7 @@ inline Tuning::Tuning(const Scale &s_, const KeyboardMapping &k_, bool allowTuni
                 {
                     if (cm > s.count)
                     {
-                        throw TuningError(std::string(
-                            "Mapping KBM note longer than scale; key=" + std::to_string(cm) +
-                            " scale count=" + std::to_string(s.count)));
+                        return;
                     }
                     push = mappingKey - cm;
                 }
@@ -1001,8 +982,7 @@ inline int Tuning::midiNoteForNoteName(std::string noteName, int octave) const
     const auto it = std::find(notationMapping.names.begin(), notationMapping.names.end(), noteName);
     if (it == notationMapping.names.end())
     {
-        std::string s = "Invalid note name '" + noteName + "'";
-        throw TuningError(s);
+        return -1;
     }
     int scalePosition = positive_mod(static_cast<int>(it - notationMapping.names.begin() + 1), notationMapping.count);
     return std::min(
@@ -1015,10 +995,9 @@ inline std::string Tuning::noteNameForScalePosition(int scalePosition) const
 {
     if (notationMapping.count == 0)
     {
-        std::string s = "No note names found in the tuning.";
-        throw TuningError(s);
+        return {};
     }
-    return notationMapping.names.at(positive_mod(scalePosition - 1, notationMapping.count));
+    return notationMapping.names[positive_mod(scalePosition - 1, notationMapping.count)];
 }
 
 inline Tuning Tuning::withSkippedNotesInterpolated() const
@@ -1146,7 +1125,7 @@ inline AbletonScale readASCLStream(std::istream &inf)
                 std::string s = "Invalid NOTE_NAMES entry '" + rawText + "': Expecting " +
                                 std::to_string(as.scale.count) + " entries but received " +
                                 std::to_string(as.notationMapping.count);
-                throw TuningError(s);
+                return as;
             }
         }
         else if (command[1] == "REFERENCE_PITCH")
@@ -1156,8 +1135,8 @@ inline AbletonScale readASCLStream(std::istream &inf)
             if (std::regex_match(rp, reference_pitch,
                                  std::regex("\\s*(\\d+)\\s*(\\d+)\\s*([\\d.]+)\\s*$")))
             {
-                as.referencePitchOctave = std::stoi(reference_pitch[1]);
-                as.referencePitchIndex = std::stoi(reference_pitch[2]);
+                as.referencePitchOctave = (int)strtol(reference_pitch[1].str().c_str(), nullptr, 10);
+                as.referencePitchIndex = (int)strtol(reference_pitch[2].str().c_str(), nullptr, 10);
                 as.referencePitchFreq = locale_atof(reference_pitch.str(3).c_str());
                 as.keyboardMapping.tuningFrequency = as.referencePitchFreq;
                 as.keyboardMapping.tuningPitch = as.keyboardMapping.tuningFrequency / MIDI_0_FREQ;
@@ -1168,8 +1147,7 @@ inline AbletonScale readASCLStream(std::istream &inf)
             }
             else
             {
-                std::string s = "Invalid REFERENCE_PITCH entry '" + rp + "'";
-                throw TuningError(s);
+                return as;
             }
         }
         else if (command[1] == "NOTE_RANGE_BY_FREQUENCY")
@@ -1190,8 +1168,7 @@ inline AbletonScale readASCLStream(std::istream &inf)
         }
         else
         {
-            std::string s = "Unhandled Ableton command '" + std::string(command[1]) + "'";
-            throw TuningError(s);
+            return as;
         }
     }
 
@@ -1254,8 +1231,7 @@ inline AbletonScale readASCLFile(std::string fname)
     inf.open(fname);
     if (!inf.is_open())
     {
-        std::string s = "Unable to open file '" + fname + "'";
-        throw TuningError(s);
+        return {};
     }
 
     auto res = readASCLStream(inf);
diff --git a/include/vrv/beamspan.h b/include/vrv/beamspan.h
index 46b5eb472..306f501d1 100644
--- a/include/vrv/beamspan.h
+++ b/include/vrv/beamspan.h
@@ -82,8 +82,16 @@ public:
      * Access the beam segments
      */
     ///@{
-    BeamSpanSegment *GetSegment(int index) { return m_beamSegments.at(index); }
-    const BeamSpanSegment *GetSegment(int index) const { return m_beamSegments.at(index); }
+    BeamSpanSegment *GetSegment(int index)
+    {
+        if (index < 0 || index >= (int)m_beamSegments.size()) return nullptr;
+        return m_beamSegments[index];
+    }
+    const BeamSpanSegment *GetSegment(int index) const
+    {
+        if (index < 0 || index >= (int)m_beamSegments.size()) return nullptr;
+        return m_beamSegments[index];
+    }
     BeamSpanSegment *GetSegmentForSystem(const System *system);
     const BeamSpanSegment *GetSegmentForSystem(const System *system) const;
     ///@}
diff --git a/include/vrv/editortoolkit_neume.h b/include/vrv/editortoolkit_neume.h
index 0f52d558f..abd34afdd 100644
--- a/include/vrv/editortoolkit_neume.h
+++ b/include/vrv/editortoolkit_neume.h
@@ -214,8 +214,8 @@ struct StaffSort {
             std::string typeB = staffB->GetType();
 
             if (staffA->GetType().find("column") == 0 && staffB->GetType().find("column") == 0) {
-                int columnA = std::stoi(typeA.substr(6));
-                int columnB = std::stoi(typeB.substr(6));
+                int columnA = (int)strtol(typeA.substr(6).c_str(), nullptr, 10);
+                int columnB = (int)strtol(typeB.substr(6).c_str(), nullptr, 10);
                 if (columnA != columnB) {
                     return columnA < columnB;
                 }
diff --git a/include/vrv/glyph.h b/include/vrv/glyph.h
index dc28607ae..97cc3d143 100644
--- a/include/vrv/glyph.h
+++ b/include/vrv/glyph.h
@@ -95,6 +95,8 @@ public:
      * The string is turn into a SMuFLGlyphAnchor ("cutOutNE" => SMUFL_cutOutNE)
      */
     void SetAnchor(std::string anchorStr, double x, double y);
+    /** Set anchor directly with pre-scaled coordinates */
+    void SetAnchor(SMuFLGlyphAnchor id, int x, int y) { m_anchors[id] = Point(x, y); }
 
     /**
      * Check if the glyph has anchor provided.
diff --git a/include/vrv/iopae.h b/include/vrv/iopae.h
index 61088f0f3..5900f56bb 100644
--- a/include/vrv/iopae.h
+++ b/include/vrv/iopae.h
@@ -15,7 +15,7 @@
  */
 //----------------------------------------------------------------------------
 //----------------------------------------------------------------------------
-// #define USE_PAE_OLD_PARSER
+#define USE_PAE_OLD_PARSER
 //----------------------------------------------------------------------------
 //----------------------------------------------------------------------------
 
diff --git a/include/vrv/object.h b/include/vrv/object.h
index 49d2f002c..70ee2c84d 100644
--- a/include/vrv/object.h
+++ b/include/vrv/object.h
@@ -8,6 +8,7 @@
 #ifndef __VRV_OBJECT_H__
 #define __VRV_OBJECT_H__
 
+#include <bitset>
 #include <cstdlib>
 #include <functional>
 #include <iterator>
@@ -147,16 +148,10 @@ public:
      * @name Methods for registering a MEI att class and for registering interfaces regrouping MEI att classes.
      */
     ///@{
-    void RegisterAttClass(AttClassId attClassId) { m_attClasses.push_back(attClassId); }
-    bool HasAttClass(AttClassId attClassId) const
-    {
-        return std::find(m_attClasses.begin(), m_attClasses.end(), attClassId) != m_attClasses.end();
-    }
+    void RegisterAttClass(AttClassId attClassId) { m_attClasses[attClassId] = true; }
+    bool HasAttClass(AttClassId attClassId) const { return m_attClasses[attClassId]; }
     void RegisterInterface(std::vector<AttClassId> *attClasses, InterfaceId interfaceId);
-    bool HasInterface(InterfaceId interfaceId) const
-    {
-        return std::find(m_interfaces.begin(), m_interfaces.end(), interfaceId) != m_interfaces.end();
-    }
+    bool HasInterface(InterfaceId interfaceId) const { return m_interfaceBits & (1u << interfaceId); }
     ///@}
 
     /**
@@ -851,15 +846,8 @@ private:
     mutable ClassId m_iteratorElementType;
     ///@}
 
-    /**
-     * A vector for storing the list of AttClassId (MEI att classes) implemented.
-     */
-    std::vector<AttClassId> m_attClasses;
-
-    /**
-     * A vector for storing the list of InterfaceId (group of MEI att classes) implemented.
-     */
-    std::vector<InterfaceId> m_interfaces;
+    std::bitset<ATT_CLASS_max> m_attClasses;
+    uint32_t m_interfaceBits = 0;
 
     /**
      * String for storing a comments attached to the object when printing an MEI element.
diff --git a/include/vrv/resources.h b/include/vrv/resources.h
index f7006b900..e64bfd44b 100644
--- a/include/vrv/resources.h
+++ b/include/vrv/resources.h
@@ -27,12 +27,19 @@ namespace vrv {
  * It manages fonts and glyph tables.
  */
 
+struct StyleAttributesHash {
+    size_t operator()(const std::pair<data_FONTWEIGHT, data_FONTSTYLE> &p) const
+    {
+        return std::hash<int>{}((static_cast<int>(p.first) << 8) | static_cast<int>(p.second));
+    }
+};
+
 class Resources {
 public:
     using StyleAttributes = std::pair<data_FONTWEIGHT, data_FONTSTYLE>;
     using GlyphTable = std::unordered_map<char32_t, Glyph>;
     using GlyphNameTable = std::unordered_map<std::string, char32_t>;
-    using GlyphTextMap = std::map<StyleAttributes, GlyphTable>;
+    using GlyphTextMap = std::unordered_map<StyleAttributes, GlyphTable, StyleAttributesHash>;
 
     /**
      * @name Constructors, destructors, and other standard methods
@@ -61,7 +68,8 @@ public:
      */
     void UseLiberationTextFont(bool useLiberation) { m_useLiberation = useLiberation; }
     bool UseLiberationTextFont() const { return m_useLiberation; }
-    std::string GetTextFont() const { return ((m_useLiberation) ? "Liberation" : "Times"); }
+    // Typst's resvg can't resolve "Times"; use Liberation Serif (metric-compatible) to avoid monospace fallback
+    std::string GetTextFont() const { return "Liberation Serif"; }
 
     /**
      * Font initialization
@@ -69,6 +77,12 @@ public:
     ///@{
     /** Init the SMufL music and text fonts */
     bool InitFonts();
+    /** Add a pre-loaded music font (for binary font loading) */
+    void AddLoadedFont(const std::string &name, GlyphTable &&table, bool isFallback);
+    /** Add a pre-loaded text font (for binary font loading) */
+    void AddTextFont(const StyleAttributes &style, GlyphTable &&table);
+    /** Set the default/current/fallback font names after loading */
+    void SetDefaultFont(const std::string &name);
     /**  Set the font to be used and loads it if necessary */
     bool SetFont(const std::string &fontName);
     /** Add custom (external) fonts */
@@ -172,14 +186,26 @@ private:
     /** Init the text font (bounding boxes and ASCII only) */
     bool InitTextFont(const std::string &fontName, const StyleAttributes &style);
 
-    const GlyphTable &GetCurrentGlyphTable() const { return m_loadedFonts.at(m_currentFontName).GetGlyphTable(); };
-    const GlyphTable &GetFallbackGlyphTable() const { return m_loadedFonts.at(m_fallbackFontName).GetGlyphTable(); };
+    const GlyphTable &GetCurrentGlyphTable() const
+    {
+        auto it = m_loadedFonts.find(m_currentFontName);
+        if (it != m_loadedFonts.end()) return it->second.GetGlyphTable();
+        static const GlyphTable empty;
+        return empty;
+    }
+    const GlyphTable &GetFallbackGlyphTable() const
+    {
+        auto it = m_loadedFonts.find(m_fallbackFontName);
+        if (it != m_loadedFonts.end()) return it->second.GetGlyphTable();
+        static const GlyphTable empty;
+        return empty;
+    }
 
     bool m_useLiberation;
     std::string m_path;
     std::string m_defaultFontName;
     std::string m_fallbackFontName;
-    std::map<std::string, LoadedFont> m_loadedFonts;
+    std::unordered_map<std::string, LoadedFont> m_loadedFonts;
     std::string m_currentFontName;
 
     /** A text font used for bounding box calculations */
diff --git a/include/vrv/svgdevicecontext.h b/include/vrv/svgdevicecontext.h
index 60bd9bc65..6a3d29633 100644
--- a/include/vrv/svgdevicecontext.h
+++ b/include/vrv/svgdevicecontext.h
@@ -12,6 +12,7 @@
 #include <list>
 #include <set>
 #include <string>
+#include <unordered_map>
 #include <vector>
 
 //----------------------------------------------------------------------------
@@ -196,7 +197,7 @@ public:
     /**
      * In SVG use global styling but not with mm output (for pdf generation)
      */
-    bool UseGlobalStyling() override { return !m_mmOutput; }
+    bool UseGlobalStyling() override { return false; }
 
     /**
      * Setting mm output flag (false by default)
@@ -380,7 +381,8 @@ private:
     };
     const std::string InsertGlyphRef(const Glyph *glyph);
     std::vector<std::pair<const Glyph *, GlyphRef>> m_smuflGlyphs;
-    std::map<std::string, int> m_glyphCodeFontCounter;
+    std::unordered_map<const Glyph *, std::string> m_smuflGlyphLookup;
+    std::unordered_map<std::string, int> m_glyphCodeFontCounter;
 
     // pugixml data
     pugi::xml_document m_svgDoc;
diff --git a/include/vrv/toolkit.h b/include/vrv/toolkit.h
index 6c846982c..e3f999020 100644
--- a/include/vrv/toolkit.h
+++ b/include/vrv/toolkit.h
@@ -816,7 +816,8 @@ private:
     void SetMidiDoc();
 
 public:
-    //
+    Doc &GetDoc() { return m_doc; }
+
 private:
     Doc m_doc;
     DocSelection m_docSelection;
diff --git a/src/alignfunctor.cpp b/src/alignfunctor.cpp
index 1ebf99ae5..8b5cc5e01 100644
--- a/src/alignfunctor.cpp
+++ b/src/alignfunctor.cpp
@@ -53,7 +53,8 @@ AlignHorizontallyFunctor::AlignHorizontallyFunctor(Doc *doc) : DocFunctor(doc)
     m_scoreDefRole = SCOREDEF_NONE;
     m_isFirstMeasure = false;
     m_hasMultipleLayer = false;
-    m_currentParams.equivalence = durationEq.at(m_doc->GetOptions()->m_durationEquivalence.GetValue());
+    auto eqIt = durationEq.find(m_doc->GetOptions()->m_durationEquivalence.GetValue());
+    m_currentParams.equivalence = (eqIt != durationEq.end()) ? eqIt->second : DURATION_brevis;
     m_sectionRestart = false;
 }
 
diff --git a/src/beam.cpp b/src/beam.cpp
index 1d219e3cf..e91ac54f2 100644
--- a/src/beam.cpp
+++ b/src/beam.cpp
@@ -369,6 +369,7 @@ bool BeamSegment::NeedToResetPosition(Staff *staff, const Doc *doc, BeamDrawingI
     // CASE 1: CROSS STAFF BEAMS
     // Those are easy, since they request staff space (See BeamSegment::RequestStaffSpace(...))
     if (beamInterface->m_crossStaffContent) {
+        if (m_beamElementCoordRefs.empty()) return false;
         data_BEAMPLACE place = m_beamElementCoordRefs.at(0)->m_beamRelativePlace;
         auto iter = std::find_if(m_beamElementCoordRefs.begin(), m_beamElementCoordRefs.end(),
             [&place](BeamElementCoord *coord) { return coord->m_beamRelativePlace != place; });
@@ -401,6 +402,7 @@ bool BeamSegment::NeedToResetPosition(Staff *staff, const Doc *doc, BeamDrawingI
     }
 
     // Calculate midpoint for the beam with mixed placement
+    if (m_beamElementCoordRefs.empty()) return false;
     int min = m_beamElementCoordRefs.at(0)->m_element->GetDrawingY();
     int max = m_beamElementCoordRefs.at(0)->m_element->GetDrawingY();
     for (BeamElementCoord *coord : m_beamElementCoordRefs) {
@@ -445,7 +447,7 @@ bool BeamSegment::NeedToResetPosition(Staff *staff, const Doc *doc, BeamDrawingI
     if ((newDirection == STEMDIRECTION_down) && (m_uniformStemLength > 0)) m_uniformStemLength *= -1;
 
     LogWarning("Insufficient space to draw mixed beam, starting at '%s'. Drawing '%s' instead.",
-        m_beamElementCoordRefs.at(0)->m_element->GetID().c_str(),
+        m_beamElementCoordRefs.empty() ? "" : m_beamElementCoordRefs.at(0)->m_element->GetID().c_str(),
         (beamInterface->m_drawingPlace == BEAMPLACE_above) ? "above" : "below");
 
     return true;
@@ -1358,7 +1360,7 @@ void BeamSegment::CalcHorizontalBeam(const Doc *doc, const Staff *staff, const B
             }
         }
 
-        if (-VRV_UNSET != abs(maxLength)) {
+        if ((-VRV_UNSET != abs(maxLength)) && !m_beamElementCoordRefs.empty()) {
             m_beamElementCoordRefs.at(0)->m_yBeam = maxLength;
         }
     }
diff --git a/src/beamspan.cpp b/src/beamspan.cpp
index 0aad46d2b..c9026f19c 100644
--- a/src/beamspan.cpp
+++ b/src/beamspan.cpp
@@ -106,15 +106,16 @@ const BeamSpanSegment *BeamSpan::GetSegmentForSystem(const System *system) const
 
 bool BeamSpan::AddSpanningSegment(const Doc *doc, const SpanIndexVector &elements, int index, bool newSegment)
 {
-    Layer *layer = vrv_cast<Layer *>((*elements.at(index).first)->GetFirstAncestor(LAYER));
-    Staff *staff = vrv_cast<Staff *>((*elements.at(index).first)->GetFirstAncestor(STAFF));
+    if (index < 0 || index >= (int)elements.size() || index + 1 >= (int)elements.size()) return false;
+    Layer *layer = vrv_cast<Layer *>((*elements[index].first)->GetFirstAncestor(LAYER));
+    Staff *staff = vrv_cast<Staff *>((*elements[index].first)->GetFirstAncestor(STAFF));
     if (!layer || !staff) return false;
 
     // get iterators for first and last coordinates in the range for the segment
     const auto coordsFirst = std::find_if(m_beamElementCoords.begin(), m_beamElementCoords.end(),
-        [&](BeamElementCoord *coord) { return coord->m_element == (*elements.at(index).first); });
+        [&](BeamElementCoord *coord) { return coord->m_element == (*elements[index].first); });
     const auto coordsLast = std::find_if(m_beamElementCoords.begin(), m_beamElementCoords.end(),
-        [&](BeamElementCoord *coord) { return coord->m_element == *(elements.at(index + 1).first - 1); });
+        [&](BeamElementCoord *coord) { return coord->m_element == *(elements[index + 1].first - 1); });
     if ((coordsFirst == m_beamElementCoords.end()) || (coordsLast == m_beamElementCoords.end())) return false;
 
     BeamSpanSegment *segment = NULL;
@@ -122,7 +123,8 @@ bool BeamSpan::AddSpanningSegment(const Doc *doc, const SpanIndexVector &element
         segment = new BeamSpanSegment();
     }
     else {
-        segment = m_beamSegments.at(0);
+        if (m_beamSegments.empty()) return false;
+        segment = m_beamSegments[0];
     }
 
     // Init segment with placement information (measure, staff, etc.) as well as begin/end coordinates
diff --git a/src/boundingbox.cpp b/src/boundingbox.cpp
index 12a18f742..55e5eb7ee 100644
--- a/src/boundingbox.cpp
+++ b/src/boundingbox.cpp
@@ -1206,16 +1206,14 @@ SegmentedLine::SegmentedLine(int start, int end)
 
 std::pair<int, int> SegmentedLine::GetStartEnd(int idx) const
 {
-    assert(idx >= 0);
-    assert(idx < this->GetSegmentCount());
+    if (idx < 0 || idx >= this->GetSegmentCount()) return { 0, 0 };
 
     if (m_increasing) {
-        return { m_segments.at(idx).first, m_segments.at(idx).second };
+        return { m_segments[idx].first, m_segments[idx].second };
     }
     else {
-        // Read the segment array "backwards"
         idx = this->GetSegmentCount() - 1 - idx;
-        return { m_segments.at(idx).second, m_segments.at(idx).first };
+        return { m_segments[idx].second, m_segments[idx].first };
     }
 }
 
diff --git a/src/calcligatureorneumeposfunctor.cpp b/src/calcligatureorneumeposfunctor.cpp
index 4ed084234..a63739fe2 100644
--- a/src/calcligatureorneumeposfunctor.cpp
+++ b/src/calcligatureorneumeposfunctor.cpp
@@ -80,8 +80,8 @@ FunctorCode CalcLigatureOrNeumePosFunctor::VisitLigature(Ligature *ligature)
         // L - L
         if ((dur1 == DURATION_long) && (dur2 == DURATION_long)) {
             if (up) {
-                ligature->m_drawingShapes.at(n1) = LIGATURE_STEM_RIGHT_DOWN;
-                ligature->m_drawingShapes.at(n2) = LIGATURE_STEM_RIGHT_DOWN;
+                ligature->m_drawingShapes[n1] = LIGATURE_STEM_RIGHT_DOWN;
+                ligature->m_drawingShapes[n2] = LIGATURE_STEM_RIGHT_DOWN;
             }
             else {
                 // nothing to change
@@ -90,14 +90,14 @@ FunctorCode CalcLigatureOrNeumePosFunctor::VisitLigature(Ligature *ligature)
         // L - B
         else if ((dur1 == DURATION_long) && (dur2 == DURATION_breve)) {
             if (up) {
-                ligature->m_drawingShapes.at(n1) = LIGATURE_STEM_RIGHT_DOWN;
+                ligature->m_drawingShapes[n1] = LIGATURE_STEM_RIGHT_DOWN;
             }
             // automatically set oblique on B, but not with Mx and only at the beginning and end
             else if (!isMaxima && ((n1 == 0) || isLastNote)) {
-                ligature->m_drawingShapes.at(n1) = LIGATURE_OBLIQUE;
+                ligature->m_drawingShapes[n1] = LIGATURE_OBLIQUE;
                 // make sure the previous one is not oblique
                 if (n1 > 0) {
-                    ligature->m_drawingShapes.at(n1 - 1) &= ~LIGATURE_OBLIQUE;
+                    ligature->m_drawingShapes[n1 - 1] &= ~LIGATURE_OBLIQUE;
                 }
             }
         }
@@ -108,38 +108,38 @@ FunctorCode CalcLigatureOrNeumePosFunctor::VisitLigature(Ligature *ligature)
             }
             // automatically set oblique on B only at the beginning and end
             else if ((n1 == 0) || isLastNote) {
-                ligature->m_drawingShapes.at(n1) = LIGATURE_OBLIQUE;
+                ligature->m_drawingShapes[n1] = LIGATURE_OBLIQUE;
                 // make sure the previous one is not oblique
                 if (n1 > 0) {
-                    ligature->m_drawingShapes.at(n1 - 1) &= ~LIGATURE_OBLIQUE;
+                    ligature->m_drawingShapes[n1 - 1] &= ~LIGATURE_OBLIQUE;
                 }
                 else {
-                    ligature->m_drawingShapes.at(n1) |= LIGATURE_STEM_LEFT_DOWN;
+                    ligature->m_drawingShapes[n1] |= LIGATURE_STEM_LEFT_DOWN;
                 }
             }
         }
         // B - L
         else if ((dur1 == DURATION_breve) && (dur2 == DURATION_long)) {
             if (up) {
-                ligature->m_drawingShapes.at(n2) = LIGATURE_STEM_RIGHT_DOWN;
+                ligature->m_drawingShapes[n2] = LIGATURE_STEM_RIGHT_DOWN;
             }
             else {
                 if (!isLastNote) {
-                    ligature->m_drawingShapes.at(n2) = LIGATURE_STEM_RIGHT_DOWN;
+                    ligature->m_drawingShapes[n2] = LIGATURE_STEM_RIGHT_DOWN;
                 }
                 if (n1 == 0) {
-                    ligature->m_drawingShapes.at(n1) = LIGATURE_STEM_LEFT_DOWN;
+                    ligature->m_drawingShapes[n1] = LIGATURE_STEM_LEFT_DOWN;
                 }
             }
         }
         // SB - SB
         else if ((dur1 == DURATION_1) && (dur2 == DURATION_1)) {
-            ligature->m_drawingShapes.at(n1) = LIGATURE_STEM_LEFT_UP;
+            ligature->m_drawingShapes[n1] = LIGATURE_STEM_LEFT_UP;
         }
         // SB - L (this should not happen on the first two notes, but this is an encoding problem)
         else if ((dur1 == DURATION_1) && (dur2 == DURATION_long)) {
             if (up) {
-                ligature->m_drawingShapes.at(n2) = LIGATURE_STEM_RIGHT_DOWN;
+                ligature->m_drawingShapes[n2] = LIGATURE_STEM_RIGHT_DOWN;
             }
             else {
                 // nothing to change
@@ -152,18 +152,18 @@ FunctorCode CalcLigatureOrNeumePosFunctor::VisitLigature(Ligature *ligature)
             }
             // only set the oblique with the SB if the following B is not the start of an oblique
             else if (note->GetLig() != LIGATUREFORM_obliqua) {
-                ligature->m_drawingShapes.at(n1) = LIGATURE_OBLIQUE;
+                ligature->m_drawingShapes[n1] = LIGATURE_OBLIQUE;
                 if (n1 > 0) {
-                    ligature->m_drawingShapes.at(n1 - 1) &= ~LIGATURE_OBLIQUE;
+                    ligature->m_drawingShapes[n1 - 1] &= ~LIGATURE_OBLIQUE;
                 }
             }
         }
 
         // Blindly set the oblique shape without trying to deal with encoding problems
         if (oblique) {
-            ligature->m_drawingShapes.at(n1) |= LIGATURE_OBLIQUE;
+            ligature->m_drawingShapes[n1] |= LIGATURE_OBLIQUE;
             if (n1 > 0) {
-                ligature->m_drawingShapes.at(n1 - 1) &= ~LIGATURE_OBLIQUE;
+                ligature->m_drawingShapes[n1 - 1] &= ~LIGATURE_OBLIQUE;
             }
         }
 
@@ -174,9 +174,9 @@ FunctorCode CalcLigatureOrNeumePosFunctor::VisitLigature(Ligature *ligature)
             // If the previous was going down, adjust the threshold
             if ((n1 > 0) && !previousUp) {
                 // For oblique, stack but only from a fourth, for recta, never stack them
-                stackThreshold = (ligature->m_drawingShapes.at(n1 - 1) & LIGATURE_OBLIQUE) ? 2 : -VRV_UNSET;
+                stackThreshold = (ligature->m_drawingShapes[n1 - 1] & LIGATURE_OBLIQUE) ? 2 : -VRV_UNSET;
             }
-            if (diatonicStep > stackThreshold) ligature->m_drawingShapes.at(n2) = LIGATURE_STACKED;
+            if (diatonicStep > stackThreshold) ligature->m_drawingShapes[n2] = LIGATURE_STACKED;
         }
 
         oblique = false;
@@ -200,7 +200,7 @@ FunctorCode CalcLigatureOrNeumePosFunctor::VisitLigature(Ligature *ligature)
         // previousRight is 0 for the first note
         int width = (note->GetDrawingRadius(m_doc, true) * 2) - m_doc->GetDrawingStemWidth(staff->m_drawingStaffSize);
         // With stacked notes, back-track the position
-        if (ligature->m_drawingShapes.at(n1 + 1) & LIGATURE_STACKED) previousRight -= width;
+        if (ligature->m_drawingShapes[n1 + 1] & LIGATURE_STACKED) previousRight -= width;
         note->SetDrawingXRel(previousRight);
         previousRight += width;
 
@@ -212,7 +212,7 @@ FunctorCode CalcLigatureOrNeumePosFunctor::VisitLigature(Ligature *ligature)
         int diatonicStep = note->GetDiatonicPitch() - previousNote->GetDiatonicPitch();
 
         // For large interval and oblique, adjust the x position to limit the angle
-        if ((ligature->m_drawingShapes.at(n1) & LIGATURE_OBLIQUE) && (abs(diatonicStep) > 2)) {
+        if ((ligature->m_drawingShapes[n1] & LIGATURE_OBLIQUE) && (abs(diatonicStep) > 2)) {
             // angle stays the same from third onward (2 / 3 or a brevis per diatonic step)
             int shift = (abs(diatonicStep) - 2) * width * 2 / 3;
             note->SetDrawingXRel(note->GetDrawingXRel() + shift);
@@ -263,39 +263,39 @@ FunctorCode CalcLigatureOrNeumePosFunctor::VisitNeume(Neume *neume)
             const int lineWidth = m_doc->GetGlyphWidth(SMUFL_E9BE_chantConnectingLineAsc3rd, staffSize, false);
 
             if (nc->GetCurve() == curvatureDirection_CURVE_c) {
-                nc->m_drawingGlyphs.at(0).m_fontNo = SMUFL_E995_chantAuctumDesc;
-                nc->m_drawingGlyphs.at(1).m_fontNo = SMUFL_E9BE_chantConnectingLineAsc3rd;
-                nc->m_drawingGlyphs.at(2).m_fontNo = SMUFL_E9BE_chantConnectingLineAsc3rd;
-                nc->m_drawingGlyphs.at(2).m_xOffset = ncWidth - lineWidth;
-                nc->m_drawingGlyphs.at(1).m_yOffset = -1.75 * unit;
-                nc->m_drawingGlyphs.at(2).m_yOffset = -1.9 * unit;
+                nc->m_drawingGlyphs[0].m_fontNo = SMUFL_E995_chantAuctumDesc;
+                nc->m_drawingGlyphs[1].m_fontNo = SMUFL_E9BE_chantConnectingLineAsc3rd;
+                nc->m_drawingGlyphs[2].m_fontNo = SMUFL_E9BE_chantConnectingLineAsc3rd;
+                nc->m_drawingGlyphs[2].m_xOffset = ncWidth - lineWidth;
+                nc->m_drawingGlyphs[1].m_yOffset = -1.75 * unit;
+                nc->m_drawingGlyphs[2].m_yOffset = -1.9 * unit;
             }
             else if (nc->GetCurve() == curvatureDirection_CURVE_a) {
-                nc->m_drawingGlyphs.at(0).m_fontNo = SMUFL_E994_chantAuctumAsc;
-                nc->m_drawingGlyphs.at(1).m_fontNo = SMUFL_E9BE_chantConnectingLineAsc3rd;
-                nc->m_drawingGlyphs.at(2).m_fontNo = SMUFL_E9BE_chantConnectingLineAsc3rd;
-                nc->m_drawingGlyphs.at(2).m_xOffset = ncWidth - lineWidth;
-                nc->m_drawingGlyphs.at(1).m_yOffset = 0.5 * unit;
-                nc->m_drawingGlyphs.at(2).m_yOffset = 0.75 * unit;
+                nc->m_drawingGlyphs[0].m_fontNo = SMUFL_E994_chantAuctumAsc;
+                nc->m_drawingGlyphs[1].m_fontNo = SMUFL_E9BE_chantConnectingLineAsc3rd;
+                nc->m_drawingGlyphs[2].m_fontNo = SMUFL_E9BE_chantConnectingLineAsc3rd;
+                nc->m_drawingGlyphs[2].m_xOffset = ncWidth - lineWidth;
+                nc->m_drawingGlyphs[1].m_yOffset = 0.5 * unit;
+                nc->m_drawingGlyphs[2].m_yOffset = 0.75 * unit;
             }
             else {
-                nc->m_drawingGlyphs.at(0).m_fontNo = SMUFL_E9A1_chantPunctumDeminutum;
+                nc->m_drawingGlyphs[0].m_fontNo = SMUFL_E9A1_chantPunctumDeminutum;
             }
         }
         else if (hasOriscus) {
-            nc->m_drawingGlyphs.at(0).m_fontNo = SMUFL_EA2A_medRenOriscusCMN;
+            nc->m_drawingGlyphs[0].m_fontNo = SMUFL_EA2A_medRenOriscusCMN;
         }
         else if (hasQuilisma) {
-            nc->m_drawingGlyphs.at(0).m_fontNo = SMUFL_E99B_chantQuilisma;
+            nc->m_drawingGlyphs[0].m_fontNo = SMUFL_E99B_chantQuilisma;
         }
         else {
-            nc->m_drawingGlyphs.at(0).m_fontNo = SMUFL_E990_chantPunctum;
+            nc->m_drawingGlyphs[0].m_fontNo = SMUFL_E990_chantPunctum;
 
             if (nc->GetLigated() == BOOLEAN_true) {
                 // This is the first nc of a ligature
                 if (!previousLig) {
                     // Temporarily set a second line glyph
-                    nc->m_drawingGlyphs.at(0).m_fontNo = SMUFL_E9B4_chantEntryLineAsc2nd;
+                    nc->m_drawingGlyphs[0].m_fontNo = SMUFL_E9B4_chantEntryLineAsc2nd;
                     previousLig = true;
                 }
                 // This is the second
@@ -304,26 +304,26 @@ FunctorCode CalcLigatureOrNeumePosFunctor::VisitNeume(Neume *neume)
                     overlapWithPrevious = false;
                     assert(previousNc);
                     previousLig = false;
-                    nc->m_drawingGlyphs.at(0).m_yOffset = -pitchDifference * unit;
-                    previousNc->m_drawingGlyphs.at(0).m_yOffset = pitchDifference * unit;
+                    nc->m_drawingGlyphs[0].m_yOffset = -pitchDifference * unit;
+                    previousNc->m_drawingGlyphs[0].m_yOffset = pitchDifference * unit;
 
                     // set the glyph for both the current and previous nc
                     switch (pitchDifference) {
                         case -1:
-                            nc->m_drawingGlyphs.at(0).m_fontNo = SMUFL_E9B9_chantLigaturaDesc2nd;
-                            previousNc->m_drawingGlyphs.at(0).m_fontNo = SMUFL_E9B4_chantEntryLineAsc2nd;
+                            nc->m_drawingGlyphs[0].m_fontNo = SMUFL_E9B9_chantLigaturaDesc2nd;
+                            previousNc->m_drawingGlyphs[0].m_fontNo = SMUFL_E9B4_chantEntryLineAsc2nd;
                             break;
                         case -2:
-                            nc->m_drawingGlyphs.at(0).m_fontNo = SMUFL_E9BA_chantLigaturaDesc3rd;
-                            previousNc->m_drawingGlyphs.at(0).m_fontNo = SMUFL_E9B5_chantEntryLineAsc3rd;
+                            nc->m_drawingGlyphs[0].m_fontNo = SMUFL_E9BA_chantLigaturaDesc3rd;
+                            previousNc->m_drawingGlyphs[0].m_fontNo = SMUFL_E9B5_chantEntryLineAsc3rd;
                             break;
                         case -3:
-                            nc->m_drawingGlyphs.at(0).m_fontNo = SMUFL_E9BB_chantLigaturaDesc4th;
-                            previousNc->m_drawingGlyphs.at(0).m_fontNo = SMUFL_E9B6_chantEntryLineAsc4th;
+                            nc->m_drawingGlyphs[0].m_fontNo = SMUFL_E9BB_chantLigaturaDesc4th;
+                            previousNc->m_drawingGlyphs[0].m_fontNo = SMUFL_E9B6_chantEntryLineAsc4th;
                             break;
                         case -4:
-                            nc->m_drawingGlyphs.at(0).m_fontNo = SMUFL_E9BC_chantLigaturaDesc5th;
-                            previousNc->m_drawingGlyphs.at(0).m_fontNo = SMUFL_E9B7_chantEntryLineAsc5th;
+                            nc->m_drawingGlyphs[0].m_fontNo = SMUFL_E9BC_chantLigaturaDesc5th;
+                            previousNc->m_drawingGlyphs[0].m_fontNo = SMUFL_E9B7_chantEntryLineAsc5th;
                             break;
                         default: break;
                     }
@@ -331,19 +331,19 @@ FunctorCode CalcLigatureOrNeumePosFunctor::VisitNeume(Neume *neume)
             }
             // Check if nc is part of a ligature or is an inclinatum
             else if (nc->HasTilt() && nc->GetTilt() == COMPASSDIRECTION_se) {
-                nc->m_drawingGlyphs.at(0).m_fontNo = SMUFL_E991_chantPunctumInclinatum;
+                nc->m_drawingGlyphs[0].m_fontNo = SMUFL_E991_chantPunctumInclinatum;
                 // No overlap with this shape
                 overlapWithPrevious = false;
             }
             // If the nc is supposed to be a virga and currently is being rendered as a punctum
             // change it to a virga
             else if (nc->GetTilt() == COMPASSDIRECTION_s
-                && nc->m_drawingGlyphs.at(0).m_fontNo == SMUFL_E990_chantPunctum) {
-                nc->m_drawingGlyphs.at(0).m_fontNo = SMUFL_E996_chantPunctumVirga;
+                && nc->m_drawingGlyphs[0].m_fontNo == SMUFL_E990_chantPunctum) {
+                nc->m_drawingGlyphs[0].m_fontNo = SMUFL_E996_chantPunctumVirga;
             }
             else if (nc->GetTilt() == COMPASSDIRECTION_n
-                && nc->m_drawingGlyphs.at(0).m_fontNo == SMUFL_E990_chantPunctum) {
-                nc->m_drawingGlyphs.at(0).m_fontNo = SMUFL_E997_chantPunctumVirgaReversed;
+                && nc->m_drawingGlyphs[0].m_fontNo == SMUFL_E990_chantPunctum) {
+                nc->m_drawingGlyphs[0].m_fontNo = SMUFL_E997_chantPunctumVirgaReversed;
             }
         }
 
@@ -358,7 +358,7 @@ FunctorCode CalcLigatureOrNeumePosFunctor::VisitNeume(Neume *neume)
             // The first glyph set the spacing - unless we are starting a ligature, in which case no spacing should be
             // added between the two nc
             if (!previousLig) {
-                xRel += m_doc->GetGlyphWidth(nc->m_drawingGlyphs.at(0).m_fontNo, staffSize, false);
+                xRel += m_doc->GetGlyphWidth(nc->m_drawingGlyphs[0].m_fontNo, staffSize, false);
             }
         }
 
diff --git a/src/customtuning.cpp b/src/customtuning.cpp
index 0c5c3e0ff..e3a289a5d 100644
--- a/src/customtuning.cpp
+++ b/src/customtuning.cpp
@@ -208,20 +208,17 @@ int CustomTuning::GetMIDIPitch(const Note *note, const int shift, const int octa
     if (note->HasOctGes()) oct = note->GetOctGes();
 
     // lookup note in the tuning and map it to a MIDI key
-    try {
-        // FIXME! Special case: when we encounter a B pitch that's in scale degree 0, increment its oct by 1
-        // otherwise, it would be in the lower octave
+    {
+        auto noteIt = m_noteMap.find(noteName);
+        if (noteIt == m_noteMap.end()) {
+            LogError("Custom tuning: Error mapping note to tuning: %s not mapped", noteName.c_str());
+            return note->GetMIDIPitch(shift, octaveShift);
+        }
         const auto it = std::find(
-            m_tuning.notationMapping.names.begin(), m_tuning.notationMapping.names.end(), m_noteMap.at(noteName));
+            m_tuning.notationMapping.names.begin(), m_tuning.notationMapping.names.end(), noteIt->second);
         int scalePosition = (it - m_tuning.notationMapping.names.begin() + 1) % m_tuning.notationMapping.count;
         if (pname == PITCHNAME_b && scalePosition == 0) oct++;
-        return m_tuning.midiNoteForNoteName(m_noteMap.at(noteName), oct);
-    }
-    catch (Tunings::TuningError &e) {
-        LogError("Custom tuning: Error mapping note to tuning: %s", e.what());
-    }
-    catch (std::out_of_range &e) {
-        LogError("Custom tuning: Error mapping note to tuning: %s not mapped", noteName.c_str());
+        return m_tuning.midiNoteForNoteName(noteIt->second, oct);
     }
     return note->GetMIDIPitch(shift, octaveShift);
 }
diff --git a/src/docselection.cpp b/src/docselection.cpp
index 5b298612d..8ea119b61 100644
--- a/src/docselection.cpp
+++ b/src/docselection.cpp
@@ -74,19 +74,19 @@ bool DocSelection::Parse(const std::string &selection)
                 }
                 else {
                     startRange = std::regex_replace(startRange, std::regex(R"([^0-9])"), "");
-                    if (!startRange.empty()) m_selectionRangeStart = std::stoi(startRange);
+                    if (!startRange.empty()) m_selectionRangeStart = (int)strtol(startRange.c_str(), nullptr, 10);
                 }
                 if (endRange == "end") {
                     m_selectionRangeEnd = -1;
                 }
                 else {
                     endRange = std::regex_replace(endRange, std::regex(R"([^0-9])"), "");
-                    if (!endRange.empty()) m_selectionRangeEnd = std::stoi(endRange);
+                    if (!endRange.empty()) m_selectionRangeEnd = (int)strtol(endRange.c_str(), nullptr, 10);
                 }
             }
             else {
                 std::string measureRangeInt = std::regex_replace(m_measureRange, std::regex(R"([^0-9])"), "");
-                if (!measureRangeInt.empty()) m_selectionRangeStart = std::stoi(measureRangeInt);
+                if (!measureRangeInt.empty()) m_selectionRangeStart = (int)strtol(measureRangeInt.c_str(), nullptr, 10);
                 m_selectionRangeEnd = m_selectionRangeStart;
             }
             // Check values
diff --git a/src/drawinginterface.cpp b/src/drawinginterface.cpp
index ae9b3f3c5..29123ea11 100644
--- a/src/drawinginterface.cpp
+++ b/src/drawinginterface.cpp
@@ -182,15 +182,16 @@ void BeamDrawingInterface::InitCoords(const ListOfObjects &childList, Staff *sta
             m_beamHasChord = true;
         }
 
-        m_beamElementCoords.at(elementCount)->m_element = current;
-        m_beamElementCoords.at(elementCount)->m_dur = currentDur;
+        if (elementCount >= (int)m_beamElementCoords.size()) break;
+        m_beamElementCoords[elementCount]->m_element = current;
+        m_beamElementCoords[elementCount]->m_dur = currentDur;
 
         // Look at beam breaks
-        m_beamElementCoords.at(elementCount)->m_breaksec = 0;
+        m_beamElementCoords[elementCount]->m_breaksec = 0;
         AttBeamSecondary *beamsecondary = dynamic_cast<AttBeamSecondary *>(current);
         if (beamsecondary && beamsecondary->HasBreaksec()) {
             if (!m_changingDur) m_changingDur = true;
-            m_beamElementCoords.at(elementCount)->m_breaksec = beamsecondary->GetBreaksec();
+            m_beamElementCoords[elementCount]->m_breaksec = beamsecondary->GetBreaksec();
         }
 
         Staff *staff = current->GetCrossStaff(layer);
diff --git a/src/hum/humlib.cpp b/src/hum/humlib.cpp
index c27fd38a4..13813c9be 100644
--- a/src/hum/humlib.cpp
+++ b/src/hum/humlib.cpp
@@ -1223,8 +1223,8 @@ HumNum Convert::kernToDuration(const string& aKernString) {
 	// classical **kern definition.  A non-standard rhythm
 	// consists of two numbers separated by any character.
 	if (hre.search(aKernString, "(\\d+)[^\\d](\\d+)")) {
-		int rtop = stoi(hre.getMatch(1));
-		int rbot = stoi(hre.getMatch(2));
+		int rtop = (int)strtol(hre.getMatch(1).c_str(), nullptr, 10);
+		int rbot = (int)strtol(hre.getMatch(2).c_str(), nullptr, 10);
 		HumNum original(rbot, rtop);  // duration is inverse
 		HumNum output(rbot, rtop);    // duration is inverse
 		original *= 4;  // adjust to quarter note count;
@@ -5638,7 +5638,7 @@ void Convert::makeBooleanTrackList(vector<bool>& spinelist,
 
 		// get first value:
 		if (hre.search(entries[i], "(\\d+)")) {
-			val = stoi(hre.getMatch(1));
+			val = (int)strtol(hre.getMatch(1).c_str(), nullptr, 10);
 		}
 		if (range) {
 			int direction = 1;
@@ -6223,7 +6223,7 @@ bool GotScore::prepareMeasures(ostream& out) {
 			// Skip lines that do not start with "s# bar"
 			continue;
 		}
-		int system = stoi(match[1]);
+		int system = (int)strtol(match[1].str().c_str(), nullptr, 10);
 		if (m_debugQ) {
 			out << ">>>>>>>>>>>>>>>>>>> PROCESSING SYSTEM = " << system << endl;
 		}
@@ -6288,7 +6288,7 @@ bool GotScore::processSystemMeasures(int barIndex, int system, ostream& out) {
 		if (!regex_search(m_cells[i][0], match, re)) {
 			continue;
 		}
-		int tsys = stoi(match[1]);
+		int tsys = (int)strtol(match[1].str().c_str(), nullptr, 10);
 		if (system != tsys) {
 			break;
 		}
@@ -6300,7 +6300,7 @@ bool GotScore::processSystemMeasures(int barIndex, int system, ostream& out) {
 		if (!regex_search(m_cells[i][0], match, rev)) {
 			continue;
 		}
-		int voiceNumber = stoi(match[1]);
+		int voiceNumber = (int)strtol(match[1].str().c_str(), nullptr, 10);
 		if (voiceNumber > m_voices) {
 			m_voices = voiceNumber;
 		}
@@ -6344,7 +6344,7 @@ bool GotScore::processSystemMeasures(int barIndex, int system, ostream& out) {
 		if (!regex_search(m_cells.at(barIndex).at(i), match, renum)) {
 			continue;
 		}
-		int barnum = stoi(match[1]);
+		int barnum = (int)strtol(match[1].str().c_str(), nullptr, 10);
 
 		if (m_debugQ) {
 			cerr << "PROCESSING MEASURE " << barnum << endl;
@@ -7998,7 +7998,7 @@ double GotScore::durationFromRhythmToken(const std::string& token) {
 	static const std::regex re(R"((\d+)(\.*))");
 	std::smatch m;
 	if (std::regex_search(token, m, re)) {
-		int base    = std::stoi(m[1].str());
+		int base    = (int)strtol(m[1].str().c_str(), nullptr, 10);
 		double dur  = 1.0 / base;
 		for (char c : m[2].str()) {
 			if (c == '.') dur += dur/2.0;
@@ -15775,7 +15775,7 @@ int HumHash::getValueInt(const string& ns1, const string& ns2,
 		int intvalue;
 		try {
 			// problem with emscripten with stoi:
-			// intvalue = stoi(value);
+			// intvalue = (int)strtol(value.c_str(), nullptr, 10);
 			stringstream converter(value);
 			if (!(converter >> intvalue)) {
 				intvalue = 0;
@@ -15879,7 +15879,7 @@ double HumHash::getValueFloat(const string& ns1, const string& ns2,
 	} else {
 		double floatvalue;
 		try {
-			floatvalue = stod(value);
+			floatvalue = strtod(value.c_str(), nullptr);
 		} catch (invalid_argument& e) {
          std::cerr << e.what() << std::endl;
 			floatvalue = 0;
@@ -18897,6 +18897,7 @@ bool HumPitch::setScientificPitch(const std::string& pitch) {
 
 
 
+
 //////////////////////////////
 //
 // HumRegex::HumRegex -- Constructor.
@@ -18918,7 +18919,7 @@ HumRegex::HumRegex(const string& exp, const string& options) {
 		// explicitly set the default syntax
 		m_regexflags = std::regex_constants::ECMAScript;
 	}
-	m_regex = regex(exp, getTemporaryRegexFlags(options));
+	m_regex = std::regex(exp, getTemporaryRegexFlags(options));
 	m_searchflags = (std::regex_constants::match_flag_type)0;
 	m_searchflags = getTemporarySearchFlags(options);
 }
@@ -19025,7 +19026,7 @@ void HumRegex::unsetGlobal(void) {
 //
 
 int HumRegex::search(const string& input, const string& exp) {
-	m_regex = regex(exp, m_regexflags);
+	m_regex = std::regex(exp, m_regexflags);
 	bool result = regex_search(input, m_matches, m_regex, m_searchflags);
 	if (!result) {
 		return 0;
@@ -19040,7 +19041,7 @@ int HumRegex::search(const string& input, const string& exp) {
 
 int HumRegex::search(const string& input, int startindex,
 		const string& exp) {
-	m_regex = regex(exp, m_regexflags);
+	m_regex = std::regex(exp, m_regexflags);
 	auto startit = input.begin() + startindex;
 	auto endit   = input.end();
 	bool result = regex_search(startit, endit, m_matches, m_regex, m_searchflags);
@@ -19069,7 +19070,7 @@ int HumRegex::search(string* input, int startindex, const string& exp) {
 
 int HumRegex::search(const string& input, const string& exp,
 		const string& options) {
-	m_regex = regex(exp, getTemporaryRegexFlags(options));
+	m_regex = std::regex(exp, getTemporaryRegexFlags(options));
 	bool result = regex_search(input, m_matches, m_regex, getTemporarySearchFlags(options));
 	if (!result) {
 		return 0;
@@ -19083,7 +19084,7 @@ int HumRegex::search(const string& input, const string& exp,
 
 int HumRegex::search(const string& input, int startindex, const string& exp,
 		const string& options) {
-	m_regex = regex(exp, getTemporaryRegexFlags(options));
+	m_regex = std::regex(exp, getTemporaryRegexFlags(options));
 	auto startit = input.begin() + startindex;
 	auto endit   = input.end();
 	bool result = regex_search(startit, endit, m_matches, m_regex, getTemporarySearchFlags(options));
@@ -19156,11 +19157,11 @@ int HumRegex::getMatchInt(int index) {
 	int output = 0;
 	if (value.size() > 0) {
 		if (isdigit(value[0])) {
-			output = std::stoi(value);
+			output = (int)strtol(value.c_str(), nullptr, 10);
 		} else if (value[0] == '-') {
-			output = std::stoi(value);
+			output = (int)strtol(value.c_str(), nullptr, 10);
 		} else if (value[0] == '+') {
-			output = std::stoi(value);
+			output = (int)strtol(value.c_str(), nullptr, 10);
 		}
 	}
 	return output;
@@ -19176,7 +19177,7 @@ int HumRegex::getMatchInt(int index) {
 double HumRegex::getMatchDouble(int index) {
 	string value = m_matches.str(index);
 	if (value.size() > 0) {
-		return stod(value);
+		return strtod(value.c_str(), nullptr);
 	} else {
 		return 0.0;
 	}
@@ -19255,14 +19256,14 @@ int HumRegex::getMatchLength(int index) {
 //
 
 bool HumRegex::match(const string& input, const string& exp) {
-	m_regex = regex(exp, m_regexflags);
+	m_regex = std::regex(exp, m_regexflags);
 	return regex_match(input, m_regex, m_searchflags);
 }
 
 
 bool HumRegex::match(const string& input, const string& exp,
 		const string& options) {
-	m_regex = regex(exp, getTemporaryRegexFlags(options));
+	m_regex = std::regex(exp, getTemporaryRegexFlags(options));
 	return regex_match(input, m_regex, getTemporarySearchFlags(options));
 }
 
@@ -19294,7 +19295,7 @@ bool HumRegex::match(const string* input, const string& exp,
 
 string& HumRegex::replaceDestructive(string& input, const string& replacement,
 		const string& exp) {
-	m_regex = regex(exp, m_regexflags);
+	m_regex = std::regex(exp, m_regexflags);
 	input = regex_replace(input, m_regex, replacement, m_searchflags);
 	return input;
 }
@@ -19311,7 +19312,7 @@ string& HumRegex::replaceDestructive(string* input, const string& replacement,
 
 string& HumRegex::replaceDestructive(string& input, const string& replacement,
 		const string& exp, const string& options) {
-	m_regex = regex(exp, getTemporaryRegexFlags(options));
+	m_regex = std::regex(exp, getTemporaryRegexFlags(options));
 	input = regex_replace(input, m_regex, replacement, getTemporarySearchFlags(options));
 	return input;
 }
@@ -19332,7 +19333,7 @@ string& HumRegex::replaceDestructive (string* input, const string& replacement,
 
 string HumRegex::replaceCopy(const string& input, const string& replacement,
 		const string& exp) {
-	m_regex = regex(exp, m_regexflags);
+	m_regex = std::regex(exp, m_regexflags);
 	string output;
 	regex_replace(std::back_inserter(output), input.begin(),
 			input.end(), m_regex, replacement);
@@ -19351,7 +19352,7 @@ string HumRegex::replaceCopy(string* input, const string& replacement,
 
 string HumRegex::replaceCopy(const string& input, const string& exp,
 		const string& replacement, const string& options) {
-	m_regex = regex(exp, getTemporaryRegexFlags(options));
+	m_regex = std::regex(exp, getTemporaryRegexFlags(options));
 	string output;
 	regex_replace(std::back_inserter(output), input.begin(),
 			input.end(), m_regex, replacement, getTemporarySearchFlags(options));
@@ -20192,7 +20193,7 @@ bool HumTransposer::setTransposition(int keyFifths, const string &semitones) {
 	if (!isValidSemitones(semitones)) {
 		return false;
 	}
-	int semis = stoi(semitones);
+	int semis = (int)strtol(semitones.c_str(), nullptr, 10);
 	return setTransposition(keyFifths, semis);
 }
 
@@ -20619,7 +20620,7 @@ int HumTransposer::getInterval(const string &intervalName) {
 		return INVALID_INTERVAL_CLASS;
 	}
 
-	int dnum = stoi(number);
+	int dnum = (int)strtol(number.c_str(), nullptr, 10);
 	if (dnum == 0) {
 		cerr << "Integer interval number cannot be zero: " << intervalName << endl;
 		return INVALID_INTERVAL_CLASS;
@@ -21608,7 +21609,7 @@ void HumTransposer::intervalToDiatonicChromatic(int &diatonic, int &chromatic, c
 		return;
 	}
 
-	int dnum = stoi(number);
+	int dnum = (int)strtol(number.c_str(), nullptr, 10);
 	if (dnum == 0) {
 		cerr << "Integer interval number cannot be zero: " << intervalName << endl;
 		chromatic = INVALID_INTERVAL_CLASS;
@@ -38343,7 +38344,7 @@ std::string HumdrumToken::getLayoutParameter(const std::string& category,
 			// later).  Also not checking validity of
 			// string first (needs to start with a digit);
 
-			int n = stoi(nparam);
+			int n = (int)strtol(nparam.c_str(), nullptr, 10);
 			if (n == subtokenindex + 1) {
 				return output;
 			} else {
@@ -38419,7 +38420,7 @@ std::string HumdrumToken::getSlurLayoutParameter(const std::string& keyname,
 
 	// currently @s requires a single value (should allow a range or multiple values later)
 	// also not checking validity of string first (needs to start with a digit);
-	int s = stoi(sparam);
+	int s = (int)strtol(sparam.c_str(), nullptr, 10);
 	if (s == subtokenindex + 1) {
 		return output;
 	} else {
@@ -38489,7 +38490,7 @@ std::string HumdrumToken::getPhraseLayoutParameter(const std::string& keyname,
 
 	// currently @s requires a single value (should allow a range or multiple values later)
 	// also not checking validity of string first (needs to start with a digit);
-	int s = stoi(sparam);
+	int s = (int)strtol(sparam.c_str(), nullptr, 10);
 	if (s == subtokenindex + 1) {
 		return output;
 	} else {
@@ -38611,7 +38612,7 @@ std::string HumdrumToken::getLayoutParameterNote(const std::string& category,
 
 	if (!nparam.empty()) {
 		// a number number is specified from the parameter(s)
-		int n = stoi(nparam);
+		int n = (int)strtol(nparam.c_str(), nullptr, 10);
 		if (n == subtokenindex + 1) {
 			return output;
 		} else {
@@ -41348,7 +41349,7 @@ string MuseData::getEncoderDate(void) {
 		string day   = hre.getMatch(2);
 		string year  = hre.getMatch(3);
 		if (year.size() == 2) {
-			int value = stoi(year);
+			int value = (int)strtol(year.c_str(), nullptr, 10);
 			if (value < 70) {
 				value += 2000;
 			} else {
@@ -42213,7 +42214,7 @@ string MuseData::getMeasureNumber(int index) {
 							// problem, so return empty string
 							return "";
 						}
-						int value = stoi(number) - 1;
+						int value = (int)strtol(number.c_str(), nullptr, 10) - 1;
 						return to_string(value);
 					}
 				}
@@ -42971,7 +42972,7 @@ int MuseRecord::getAttributeInt(char attribute) {
 	} else {
 		string value = &getColumn(column+1);
 		if (value.empty()) {
-			output = std::stoi(value);
+			output = (int)strtol(value.c_str(), nullptr, 10);
 			return output;
 		} else {
 			return 0;
@@ -43283,7 +43284,7 @@ int MuseRecord::getFigureDuration(void) {
 		return output;
 	} else {
 		try {
-			output = std::stoi(value);
+			output = (int)strtol(value.c_str(), nullptr, 10);
 		} catch (const std::invalid_argument& e) {
         cout << "Invalid integer: " << e.what() << ". Setting to 0." << endl;
         output = 0;
@@ -44353,7 +44354,7 @@ int MuseRecord::getTickDuration(void) {
 	if (recordInfo.empty()) {
 		return 0;
 	}
-	return std::stoi(recordInfo);
+	return (int)strtol(recordInfo.c_str(), nullptr, 10);
 }
 
 
@@ -44373,7 +44374,7 @@ int MuseRecord::getLineTickDuration(void) {
 	if (recordInfo.empty()) {
 		return 0;
 	}
-	int value = std::stoi(recordInfo);
+	int value = (int)strtol(recordInfo.c_str(), nullptr, 10);
 	if (getType() == E_muserec_backspace) {
 		return -value;
 	}
@@ -44394,7 +44395,7 @@ int MuseRecord::getTicks(void) {
 	if (recordInfo.empty()) {
 		return 0;
 	}
-	int value = std::stoi(recordInfo);
+	int value = (int)strtol(recordInfo.c_str(), nullptr, 10);
 	if (getType() == E_muserec_backspace) {
 		return -value;
 	}
@@ -44415,7 +44416,7 @@ int MuseRecord::getNoteTickDuration(void) {
 	if (recordInfo.empty()) {
 		return value;
 	}
-	value = std::stoi(recordInfo);
+	value = (int)strtol(recordInfo.c_str(), nullptr, 10);
 	if (getType() == E_muserec_backspace) {
 		return -value;
 	}
@@ -49464,7 +49465,7 @@ int MxmlEvent::hasSlurStart(vector<int>& directions) {
 					// for now ignore the slur numbers
 					//string number = grandchild.attribute("number").value();
 					//if (!number.empty()) {
-					//	int num = stoi(number);
+					//	int num = (int)strtol(number.c_str(), nullptr, 10);
 					//	if (num != 0) {
 					//		xxx = num;
 					//	}
@@ -49527,7 +49528,7 @@ int MxmlEvent::hasSlurStop(void) {
 							output++;
 							//string number = grandchild.attribute("number").value();
 							//if (!number.empty()) {
-							//	int num = stoi(number);
+							//	int num = (int)strtol(number.c_str(), nullptr, 10);
 							//	if (num != 0) {
 							//		return num;
 							//	} else {
@@ -50554,7 +50555,7 @@ void MxmlEvent::addNotations(stringstream& ss, xml_node notations,
 					}
 					if (ttype != "stop") {
 						string tstring = grandchild.child_value();
-						tremolo = 1 << (stoi(tstring) + 2);
+						tremolo = 1 << ((int)strtol(tstring.c_str(), nullptr, 10) + 2);
 					}
 				}
 
@@ -50784,7 +50785,7 @@ string MxmlEvent::getRestPitch(void) const {
 		return "";
 	}
 
-	int octaveval = stoi(octavetext);
+	int octaveval = (int)strtol(octavetext.c_str(), nullptr, 10);
 	int count = 1;
 	char pc = steptext[0];
 	if (octaveval > 3) {
@@ -84065,22 +84066,21 @@ void Tool_esac2hum::convertEsacToHumdrum(ostream& output, istream& infile) {
 
 	while (!infile.eof()) {
 		if (m_debugQ) {
-			cerr << "Getting a song..." << endl;
+			//cerr << "Getting a song..." << endl;
 		}
 		bool status = getSong(song, infile);
 		if (!status) {
-			cerr << "Error getting a song" << endl;
 			continue;
 		}
 		if (m_debugQ) {
-			cerr << "Got a song ..." << endl;
+			//cerr << "Got a song ..." << endl;
 		}
 		if (song.empty()) {
-			cerr << "Song is empty" << endl;
+			//cerr << "Song is empty" << endl;
 			continue;
 		}
 		if (song.size() < 4) {
-			cerr << "Song is too short" << endl;
+			//cerr << "Song is too short" << endl;
 			continue;
 		}
 		convertSong(output, song);
@@ -84170,11 +84170,11 @@ bool Tool_esac2hum::getSong(vector<string>& song, istream& infile) {
 		cleanText(buffer);
 		m_inputline++;
 		if (m_debugQ) {
-			cerr << "READ LINE: " << buffer << endl;
+			//cerr << "READ LINE: " << buffer << endl;
 		}
 		if (expectingCloseQ) {
 			if (buffer.find("[") != string::npos) {
-				cerr << "Strange error on line " << m_inputline << ": " << buffer << endl;
+				//cerr << "Strange error on line " << m_inputline << ": " << buffer << endl;
 				continue;
 			} else if (!hre.search(buffer, "[\\[\\]]")) {
 				// intermediate parameter line (not starting or ending)
@@ -84188,7 +84188,7 @@ bool Tool_esac2hum::getSong(vector<string>& song, istream& infile) {
 				song.push_back(buffer);
 				continue;
 			} else {
-				cerr << "STRANGE CASE HERE " << buffer << endl;
+				//cerr << "STRANGE CASE HERE " << buffer << endl;
 			}
 			continue;
 		}
@@ -84212,7 +84212,7 @@ bool Tool_esac2hum::getSong(vector<string>& song, istream& infile) {
 	}
 
 	if (expectingCloseQ) {
-		cerr << "Strange case: expecting closing of a song parameter around line " << m_inputline++ << endl;
+		//cerr << "Strange case: expecting closing of a song parameter around line " << m_inputline++ << endl;
 	}
 
 	return true;
@@ -84354,7 +84354,7 @@ void Tool_esac2hum::printScoreContents(ostream& output) {
 	vector<string>& errors = m_score.m_errors;
 	if (!errors.empty()) {
 		for (int z=0; z<(int)errors.size(); z++) {
-			output << "!!" << errors.at(z) << endl;
+			output << "!!" << errors[z] << endl;
 		}
 	}
 
@@ -84372,7 +84372,7 @@ void Tool_esac2hum::printScoreContents(ostream& output) {
 	}
 
 	for (int i=0; i<(int)m_score.size(); i++) {
-		Tool_esac2hum::Phrase& phrase = m_score.at(i);
+		Tool_esac2hum::Phrase& phrase = m_score[i];
 		if (m_verbose.find("p") != string::npos) {
 			output << "!!esac-phrase: " << phrase.esac;
 			if (m_verbose.find("pi") != string::npos) {
@@ -84383,7 +84383,7 @@ void Tool_esac2hum::printScoreContents(ostream& output) {
 			vector<string>& errors = phrase.m_errors;
 			if (!errors.empty()) {
 				for (int z=0; z<(int)errors.size(); z++) {
-					output << "!!" << errors.at(z) << endl;
+					output << "!!" << errors[z] << endl;
 				}
 			}
 			output << endl;
@@ -84391,7 +84391,7 @@ void Tool_esac2hum::printScoreContents(ostream& output) {
 
 		for (int j=0; j<(int)phrase.size(); j++) {
 
-			Tool_esac2hum::Measure& measure = phrase.at(j);
+			Tool_esac2hum::Measure& measure = phrase[j];
 			if ((j == 0) && (i > 0)) {
 				output << "!!LO:LB:g=esac" << endl;
 			}
@@ -84432,7 +84432,7 @@ void Tool_esac2hum::printScoreContents(ostream& output) {
 			vector<string>& errors = measure.m_errors;
 			if (!errors.empty()) {
 				for (int z=0; z<(int)errors.size(); z++) {
-					output << "!!" << errors.at(z) << endl;
+					output << "!!" << errors[z] << endl;
 				}
 			}
 
@@ -84443,7 +84443,7 @@ void Tool_esac2hum::printScoreContents(ostream& output) {
 
 			for (int k=0; k<(int)measure.size(); k++) {
 
-				Tool_esac2hum::Note& note = measure.at(k);
+				Tool_esac2hum::Note& note = measure[k];
 				if (m_verbose.find("n") != string::npos) {
 					output << "!!esac-note: " << note.esac;
 					if (m_verbose.find("ni") != string::npos) {
@@ -84457,7 +84457,7 @@ void Tool_esac2hum::printScoreContents(ostream& output) {
 					vector<string>& errors = note.m_errors;
 					if (!errors.empty()) {
 						for (int z=0; z<(int)errors.size(); z++) {
-							output << "!!" << errors.at(z) << endl;
+							output << "!!" << errors[z] << endl;
 						}
 					}
 					output << endl;
@@ -84489,7 +84489,7 @@ bool Tool_esac2hum::Score::parseMel(const string& mel) {
 	HumRegex hre;
 	if (hre.search(mel, "^\\s*$")) {
 		// no data;
-		cerr << "ERROR: MEL parameter is empty or non-existent" << endl;
+		//cerr << "ERROR: MEL parameter is empty or non-existent" << endl;
 		return false;
 	}
 
@@ -84509,7 +84509,7 @@ bool Tool_esac2hum::Score::parseMel(const string& mel) {
 		string unknown = line;
 		hre.replaceDestructive(unknown, "", "[\\^0-9b\\s/._#()+-]+", "g");
 		if (!unknown.empty()) {
-			cerr << "Unknown characters " << ">>" << unknown << "<< " << " on mel line " << lineNumber << ": " << line << endl;
+			//cerr << "Unknown characters " << ">>" << unknown << "<< " << " on mel line " << lineNumber << ": " << line << endl;
 		}
 		line = Tool_esac2hum::trimSpaces(line);
 		lines.push_back(line);
@@ -84532,7 +84532,7 @@ bool Tool_esac2hum::Score::parseMel(const string& mel) {
 		}
 	}
 	if (lines.empty()) {
-		cerr << "ERROR: No notes in MEL data" << endl;
+		//cerr << "ERROR: No notes in MEL data" << endl;
 		return false;
 	}
 
@@ -84797,7 +84797,7 @@ void Tool_esac2hum::Score::prepareMultipleTimeSignatures(const string& ts) {
 	string timesigs = ts;
 	hre.split(tss, timesigs, "\\s+");
 	if (tss.size() < 2) {
-		cerr << "Time sigs: " << ts << " needs to have at least two time signatures" << endl;
+		//cerr << "Time sigs: " << ts << " needs to have at least two time signatures" << endl;
 	}
 
 	// Calculate tick duration of time signature in list:
@@ -84823,7 +84823,7 @@ void Tool_esac2hum::Score::prepareMultipleTimeSignatures(const string& ts) {
 	for (int i=0; i<(int)size(); i++) {
 		Tool_esac2hum::Phrase& phrase = at(i);
 		for (int j=1; j<(int)phrase.size()-1; j++) {
-			Tool_esac2hum::Measure& measure = phrase.at(j);
+			Tool_esac2hum::Measure& measure = phrase[j];
 			for (int k=0; k<(int)tss.size(); k++) {
 				if (tsticks[k] == measure.m_ticks) {
 					measure.m_measureTimeSignature = "*M" + tss[k];
@@ -84848,7 +84848,7 @@ void Tool_esac2hum::Score::prepareMultipleTimeSignatures(const string& ts) {
 			continue;
 		}
 		Tool_esac2hum::Measure& measure = phrase.back();
-		Tool_esac2hum::Measure& nextmeasure = nextphrase.at(0);
+		Tool_esac2hum::Measure& nextmeasure = nextphrase[0];
 
 		int mticks  = measure.m_ticks;
 		int nmticks = nextmeasure.m_ticks;
@@ -84857,10 +84857,10 @@ void Tool_esac2hum::Score::prepareMultipleTimeSignatures(const string& ts) {
 		int found2 = -1;
 
 		for (int j=(int)tss.size() - 1; j>=0; j--) {
-			if (tsticks.at(j) == mticks) {
+			if (tsticks[j] == mticks) {
 				found1 = j;
 			}
-			if (tsticks.at(j) == nmticks) {
+			if (tsticks[j] == nmticks) {
 				found2 = j;
 			}
 		}
@@ -84876,10 +84876,10 @@ void Tool_esac2hum::Score::prepareMultipleTimeSignatures(const string& ts) {
 			// form two partial measures.
 			int ticksum = mticks + nmticks;
 			for (int z=0; z<(int)tsticks.size(); z++) {
-				if (tsticks.at(z) == ticksum) {
+				if (tsticks[z] == ticksum) {
 					nextmeasure.m_barnum = -1;
-					measure.m_measureTimeSignature = "*M" + tss.at(z);
-					nextmeasure.m_measureTimeSignature = "*M" + tss.at(z);
+					measure.m_measureTimeSignature = "*M" + tss[z];
+					nextmeasure.m_measureTimeSignature = "*M" + tss[z];
 					measure.setPartialBegin();
 					nextmeasure.setPartialEnd();
 				}
@@ -84893,10 +84893,10 @@ void Tool_esac2hum::Score::prepareMultipleTimeSignatures(const string& ts) {
 	// last measure and partial end for first measure.
 	if (empty()) {
 		// no data
-	} else if ((size() == 1) && (at(0).size() <= 1)) {
+	} else if ((size() == 1) && ((*this)[0].size() <= 1)) {
 		// single measure in melody
 	} else {
-		Tool_esac2hum::Measure& firstmeasure = at(0).at(0);
+		Tool_esac2hum::Measure& firstmeasure = (*this)[0][0];
 		Tool_esac2hum::Measure& lastmeasure  = back().back();
 
 		double firstticks = firstmeasure.m_ticks;
@@ -84906,18 +84906,18 @@ void Tool_esac2hum::Score::prepareMultipleTimeSignatures(const string& ts) {
 		int foundlast  = -1;
 
 		for (int i=(int)tss.size() - 1; i>=0; i--) {
-			if (tsticks.at(i) == firstticks) {
+			if (tsticks[i] == firstticks) {
 				foundfirst = i;
 			}
-			if (tsticks.at(i) == lastticks) {
+			if (tsticks[i] == lastticks) {
 				foundlast = i;
 			}
 		}
 
 		if ((foundfirst >= 0) && (foundlast >= 0)) {
 			// first and last measures are both complete
-			firstmeasure.m_measureTimeSignature = "*M" + tss.at(foundfirst);
-			lastmeasure.m_measureTimeSignature = "*M" + tss.at(foundlast);
+			firstmeasure.m_measureTimeSignature = "*M" + tss[foundfirst];
+			lastmeasure.m_measureTimeSignature = "*M" + tss[foundlast];
 			firstmeasure.setComplete();
 			lastmeasure.setComplete();
 		} else {
@@ -84935,8 +84935,8 @@ void Tool_esac2hum::Score::prepareMultipleTimeSignatures(const string& ts) {
 				// use that time signture for both, mark firt measure
 				// last pickup (barnum -> 0), and mark last as partial
 				// measure start
-				firstmeasure.m_measureTimeSignature = "*M" + tss.at(sumfound);
-				lastmeasure.m_measureTimeSignature = "*M" + tss.at(sumfound);
+				firstmeasure.m_measureTimeSignature = "*M" + tss[sumfound];
+				lastmeasure.m_measureTimeSignature = "*M" + tss[sumfound];
 				firstmeasure.m_barnum = 0;
 				firstmeasure.setPartialEnd();
 				lastmeasure.setPartialBegin();
@@ -84957,28 +84957,28 @@ void Tool_esac2hum::Score::prepareMultipleTimeSignatures(const string& ts) {
 	getMeasureList(measurelist);
 	int barnum = 1;
 	for (int i=0; i<(int)measurelist.size(); i++) {
-		if ((i == 0) && measurelist.at(i)->isPartialEnd()) {
-			measurelist.at(i)->m_barnum = 0;
+		if ((i == 0) && measurelist[i]->isPartialEnd()) {
+			measurelist[i]->m_barnum = 0;
 			continue;
 		}
-		if (measurelist.at(i)->isComplete()) {
-			measurelist.at(i)->m_barnum = barnum++;
-		} else if (measurelist.at(i)->isPartialBegin()) {
-			measurelist.at(i)->m_barnum = barnum++;
-		} else if (measurelist.at(i)->isPartialEnd()) {
-			measurelist.at(i)->m_barnum = -1;
+		if (measurelist[i]->isComplete()) {
+			measurelist[i]->m_barnum = barnum++;
+		} else if (measurelist[i]->isPartialBegin()) {
+			measurelist[i]->m_barnum = barnum++;
+		} else if (measurelist[i]->isPartialEnd()) {
+			measurelist[i]->m_barnum = -1;
 		} else {
-			measurelist.at(i)->m_errors.push_back("UNCATEGORIZED MEASURE");
+			measurelist[i]->m_errors.push_back("UNCATEGORIZED MEASURE");
 		}
 	}
 
 	// Now remove duplicate time signatures
 	string current = "";
 	for (int i=0; i<(int)measurelist.size(); i++) {
-		if (measurelist.at(i)->m_measureTimeSignature == current) {
-			measurelist.at(i)->m_measureTimeSignature = "";
+		if (measurelist[i]->m_measureTimeSignature == current) {
+			measurelist[i]->m_measureTimeSignature = "";
 		} else {
-			current = measurelist.at(i)->m_measureTimeSignature;
+			current = measurelist[i]->m_measureTimeSignature;
 		}
 	}
 
@@ -85015,7 +85015,7 @@ void Tool_esac2hum::Score::calculateKeyInformation(void) {
 	for (int i=0; i<(int)notelist.size(); i++) {
 		int pc = notelist[i]->m_b40degree;
 		if ((pc >= 0) && (pc < 40)) {
-			b40pcs.at(pc)++;
+			b40pcs[pc]++;
 		}
 	}
 
@@ -85028,8 +85028,8 @@ void Tool_esac2hum::Score::calculateKeyInformation(void) {
 	char letter = std::toupper(tonic[0]);
 
 	// Compare counts of third and sixth pitch classes:
-	int majorsum = b40pcs.at(12) + b40pcs.at(29);
-	int minorsum = b40pcs.at(11) + b40pcs.at(28);
+	int majorsum = b40pcs[12] + b40pcs[29];
+	int minorsum = b40pcs[11] + b40pcs[28];
 	if (minorsum > majorsum) {
 		letter = std::tolower(letter);
 	}
@@ -85227,11 +85227,11 @@ void Tool_esac2hum::Score::generateHumdrumNotes(void) {
 		return;
 	}
 
-	m_minrhy = std::stoi(minrhy);
+	m_minrhy = (int)strtol(minrhy.c_str(), nullptr, 10);
 	// maybe check of power of two?
 
 	for (int i=0; i<(int)notelist.size(); i++) {
-		notelist.at(i)->generateHumdrum(m_minrhy, m_b40tonic);
+		notelist[i]->generateHumdrum(m_minrhy, m_b40tonic);
 	}
 
 }
@@ -85318,15 +85318,15 @@ void Tool_esac2hum::Score::analyzeTies(void) {
 
 	for (int i=1; i<(int)notelist.size(); i++) {
 		// negative m_degree indicates a tied note to previous note
-		if (notelist.at(i)->m_degree < 0) {
+		if (notelist[i]->m_degree < 0) {
 			// Tied note, so link to previous note.
-			notelist.at(i)->m_tieEnd = true;
-			notelist.at(i-1)->m_tieBegin = true;
-			if (notelist.at(i-1)->m_degree >= 0) {
-				notelist.at(i)->m_degree = -notelist.at(i-1)->m_degree;
+			notelist[i]->m_tieEnd = true;
+			notelist[i-1]->m_tieBegin = true;
+			if (notelist[i-1]->m_degree >= 0) {
+				notelist[i]->m_degree = -notelist[i-1]->m_degree;
 				// Copy chromatic alteration and octave:
-				notelist[i]->m_alter = notelist.at(i-1)->m_alter;
-				notelist[i]->m_octave = notelist.at(i-1)->m_octave;
+				notelist[i]->m_alter = notelist[i-1]->m_alter;
+				notelist[i]->m_octave = notelist[i-1]->m_octave;
 			}
 		}
 	}
@@ -85347,7 +85347,7 @@ void Tool_esac2hum::Score::getNoteList(vector<Tool_esac2hum::Note*>& notelist) {
 		for (int j=0; j<(int)phrase.size(); j++) {
 			Tool_esac2hum::Measure& measure = phrase[j];
 			for (int k=0; k<(int)measure.size(); k++) {
-				notelist.push_back(&measure.at(k));
+				notelist.push_back(&measure[k]);
 			}
 		}
 	}
@@ -85394,11 +85394,11 @@ void Tool_esac2hum::Score::analyzePhrases(void) {
 		// Find the first non-rest note and mark with phrase start:
 		bool foundNote = false;
 		for (int j=0; j<(int)notelist.size(); j++) {
-			if (notelist.at(j)->m_degree <= 0) {
+			if (notelist[j]->m_degree <= 0) {
 				continue;
 			}
 			foundNote = true;
-			notelist.at(j)->m_phraseBegin = true;
+			notelist[j]->m_phraseBegin = true;
 			break;
 		}
 
@@ -85409,10 +85409,10 @@ void Tool_esac2hum::Score::analyzePhrases(void) {
 
 		// Find the last non-rest note and mark with phrase end:
 		for (int j=(int)notelist.size()-1; j>=0; j--) {
-			if (notelist.at(j)->m_degree <= 0) {
+			if (notelist[j]->m_degree <= 0) {
 				continue;
 			}
-			notelist.at(j)->m_phraseEnd = true;
+			notelist[j]->m_phraseEnd = true;
 			break;
 		}
 	}
@@ -85440,7 +85440,7 @@ void Tool_esac2hum::Score::getPhraseNoteList(vector<Tool_esac2hum::Note*>& notel
 	for (int i=0; i<(int)phrase.size(); i++) {
 		Tool_esac2hum::Measure& measure = phrase[i];
 		for (int j=0; j<(int)measure.size(); j++) {
-			Tool_esac2hum::Note& note = measure.at(j);
+			Tool_esac2hum::Note& note = measure[j];
 			notelist.push_back(&note);
 		}
 	}
@@ -85461,7 +85461,7 @@ void Tool_esac2hum::Phrase::getNoteList(vector<Tool_esac2hum::Note*>& notelist)
 	for (int i=0; i<(int)phrase.size(); i++) {
 		Tool_esac2hum::Measure& measure = phrase[i];
 		for (int j=0; j<(int)measure.size(); j++) {
-			Tool_esac2hum::Note& note = measure.at(j);
+			Tool_esac2hum::Note& note = measure[j];
 			notelist.push_back(&note);
 		}
 	}
@@ -85484,7 +85484,7 @@ bool Tool_esac2hum::Phrase::parsePhrase(const string& phrase) {
 	newphrase = trimSpaces(newphrase);
 	hre.split(bars, newphrase, "\\s+");
 	if (bars.empty()) {
-		cerr << "Funny error with no measures" << endl;
+		//cerr << "Funny error with no measures" << endl;
 		return false;
 	}
 	int length = (int)bars.size();
@@ -85559,7 +85559,7 @@ bool Tool_esac2hum::Measure::parseMeasure(const string& measure) {
 			if (!tokens.empty()) {
 				tokens.back() += measure[i];
 			} else {
-				cerr << "!!ERROR: unknown character at start of measure: " << measure << endl;
+				//cerr << "!!ERROR: unknown character at start of measure: " << measure << endl;
 			}
 		}
 
@@ -85569,7 +85569,7 @@ bool Tool_esac2hum::Measure::parseMeasure(const string& measure) {
 	}
 
 	if (tokens.empty()) {
-		cerr << "!!ERROR: In measure: " << measure << ": no notes to parts." << endl;
+		//cerr << "!!ERROR: In measure: " << measure << ": no notes to parts." << endl;
 		return false;
 	}
 
@@ -85630,10 +85630,10 @@ bool Tool_esac2hum::Note::parseNote(const string& note, HumNum factor) {
 	}
 
 	if (b > 2) {
-		cerr << "!! ERROR: more than double flat not parseable, note: " << esac << endl;
+		//cerr << "!! ERROR: more than double flat not parseable, note: " << esac << endl;
 	}
 	if (s > 2) {
-		cerr << "!! ERROR: more than double sharp not parseable, note: " << esac << endl;
+		//cerr << "!! ERROR: more than double sharp not parseable, note: " << esac << endl;
 	}
 
 	m_alter = s - b;
@@ -85852,18 +85852,18 @@ void Tool_esac2hum::getParameters(vector<string>& infile) {
 		}
 		if (expectingCloseQ) {
 			if (infile[i].find("[") != string::npos) {
-				cerr << "Strange case searching for close: " << infile[i] << endl;
+				//cerr << "Strange case searching for close: " << infile[i] << endl;
 			} else if (infile[i].find("]") == string::npos) {
 				// continuing a parameter:
 				if (lastKey == "") {
-					cerr << "Strange case of no last key when closing parameter: " << infile[i] << endl;
+					//cerr << "Strange case of no last key when closing parameter: " << infile[i] << endl;
 				} else {
 					m_score.m_params[lastKey] += "\n" + infile[i];
 				}
 			} else if (hre.search(infile[i], "^([^\\]]+)\\]\\s*$")) {
 				// closing a parameter:
 				if (lastKey == "") {
-					cerr << "Strange case B of no last key when closing parameter: " << infile[i] << endl;
+					//cerr << "Strange case B of no last key when closing parameter: " << infile[i] << endl;
 				} else {
 					string value = hre.getMatch(1);
 					m_score.m_params[lastKey] += "\n" + value;
@@ -85871,7 +85871,7 @@ void Tool_esac2hum::getParameters(vector<string>& infile) {
 					continue;
 				}
 			} else {
-				cerr << "Problem closing parameter: " << infile[i] << endl;
+				//cerr << "Problem closing parameter: " << infile[i] << endl;
 			}
 			continue;
 		} else if (hre.search(infile[i], "^\\s*([A-Z_a-z]+)\\s*\\[([^\\]]*)\\]\\s*$")) {
@@ -85902,7 +85902,7 @@ void Tool_esac2hum::getParameters(vector<string>& infile) {
 			// Do nothing: an external comment, or embedded filter processed
 			// when filter loading the file.
 		} else {
-			cerr << "UNKNOWN CASE: " << infile[i] << endl;
+			//cerr << "UNKNOWN CASE: " << infile[i] << endl;
 		}
 	}
 
@@ -85926,10 +85926,10 @@ void Tool_esac2hum::getParameters(vector<string>& infile) {
 		string tonic  = hre.getMatch(3);
 		if (tonic.size() >= 1) {
 			if (tonic[0] == 'b') {
-				cerr << "Error: key signature cannot be 'b'." << endl;
+				//cerr << "Error: key signature cannot be 'b'." << endl;
 			} else {
 				if (std::islower(tonic[0])) {
-					cerr << "Warning: Tonic note should be upper case." << endl;
+					//cerr << "Warning: Tonic note should be upper case." << endl;
 					tonic[0] = std::toupper(tonic[0]);
 				}
 			}
@@ -85939,9 +85939,9 @@ void Tool_esac2hum::getParameters(vector<string>& infile) {
 		m_score.m_params["_minrhy"] = minrhy;
 		m_score.m_params["_tonic"]  = tonic;
 		m_score.m_params["_time"]   = time;
-		m_minrhy = stoi(minrhy);
+		m_minrhy = (int)strtol(minrhy.c_str(), nullptr, 10);
 	} else {
-		cerr << "Problem parsing KEY parameter: " << key << endl;
+		//cerr << "Problem parsing KEY parameter: " << key << endl;
 	}
 
 	string trd = m_score.m_params["TRD"];
@@ -85979,7 +85979,7 @@ void Tool_esac2hum::getParameters(vector<string>& infile) {
 	} else if (hre.search(trd, "\\bs,\\s*(\\d+)", "i")) {
 		// s, #
 	} else {
-		cerr << "CANNOT FIND PAGE NUMBER IN " << trd << endl;
+		//cerr << "CANNOT FIND PAGE NUMBER IN " << trd << endl;
 	}
 
 	if (m_debugQ) {
@@ -86002,13 +86002,13 @@ void Tool_esac2hum::getParameters(vector<string>& infile) {
 //
 
 void Tool_esac2hum::printParameters(void) {
-	cerr << endl;
-	cerr << "========================================" << endl;
+	//cerr << endl;
+	//cerr << "========================================" << endl;
 	for (const auto& [key, value] : m_score.m_params) {
-		cerr << "Key: " << key << ", Value: " << value << endl;
+		//cerr << "Key: " << key << ", Value: " << value << endl;
 	}
-	cerr << "========================================" << endl;
-	cerr << endl;
+	//cerr << "========================================" << endl;
+	//cerr << endl;
 }
 
 
@@ -86058,7 +86058,7 @@ void Tool_esac2hum::printFooter(ostream& output, vector<string>& infile) {
 
 	if (!m_globalComments.empty()) {
 		for (int i=0; i<(int)m_globalComments.size(); i++) {
-			output << m_globalComments.at(i) << endl;
+			output << m_globalComments[i] << endl;
 		}
 	}
 }
@@ -86441,17 +86441,17 @@ int Tool_esac2hum::Phrase::getFullMeasureCount(void) {
 	if (measureCount < 2) {
 		return measureCount;
 	}
-	if (at(0).isPartialEnd() && back().isPartialBegin()) {
+	if ((*this)[0].isPartialEnd() && back().isPartialBegin()) {
 		measureCount--;
 	}
 
 	// if the fist is partial and the last is not, also -1
-	if (at(0).isPartialEnd() && back().isComplete()) {
+	if ((*this)[0].isPartialEnd() && back().isComplete()) {
 		measureCount--;
 	}
 
 	// if the fist is complete and the last is incomplete, also -1
-	if (at(0).isComplete() && back().isPartialBegin()) {
+	if ((*this)[0].isComplete() && back().isPartialBegin()) {
 		measureCount--;
 	}
 
@@ -86570,9 +86570,9 @@ void Tool_esac2hum::Score::analyzeACC(void) {
 	for (int i=0; i<(int)size(); i++) {
 		Tool_esac2hum::Phrase& phrase = at(i);
 		for (int j=0; j<(int)phrase.size(); j++) {
-			Tool_esac2hum::Measure& measure = phrase.at(j);
+			Tool_esac2hum::Measure& measure = phrase[j];
 			if (measure.isComplete()) {
-				output += measure.at(0).getScaleDegree();
+				output += measure[0].getScaleDegree();
 			}
 		}
 		if (i < (int)size() -1) {
@@ -86727,10 +86727,10 @@ string Tool_esac2hum::getKolbergUrl(int volume) {
 		if (hre.search(pageinfo, "(\\d+)")) {
 			printPage = hre.getMatchInt(1);
 		} else {
-		     cerr << "XX PRINT PAGE: " << printPage << "\t_page: " << pageinfo << endl;
+		     //cerr << "XX PRINT PAGE: " << printPage << "\t_page: " << pageinfo << endl;
 		}
 	} else {
-		cerr << "YY PRINT PAGE: " << printPage << "\t_page: IS EMPTY: " << pageinfo << endl;
+		//cerr << "YY PRINT PAGE: " << printPage << "\t_page: IS EMPTY: " << pageinfo << endl;
 	}
 
 	// Calculate the scan page that matches with the print page:
@@ -97982,8 +97982,8 @@ void Tool_humsort::processFile(HumdrumFile& infile) {
 				char chb = b->at(0);
 				if ((isdigit(cha) || cha == '-' || cha == '+' || cha == '.') &&
 				    (isdigit(chb) || chb == '-' || chb == '+' || chb == '.')) {
-					int A = stod(*a);
-					int B = stod(*b);
+					int A = strtod(a->c_str(), nullptr);
+					int B = strtod(b->c_str(), nullptr);
 					if (A < B) {
 						return -1;
 					} else {
@@ -98712,7 +98712,7 @@ bool Tool_imitation::run(HumdrumFile& infile) {
 		hre.split(values, intstring.c_str(), "[^0-9+-]+");
 		m_intervals.resize(values.size());
 		for (int i=0; i<(int)values.size(); i++) {
-			m_intervals.at(i) = stoi(values.at(i));
+			m_intervals.at(i) = (int)strtol(values.at(i).c_str(), nullptr, 10);
 			// subtract one since intervals in caluculations are zero-indexed:
 			if (m_intervals.at(i) > 0) {
 				m_intervals.at(i)--;
@@ -101153,22 +101153,22 @@ void Tool_mei2hum::processHairpin(hairpin_info& info) {
 		myit++;
 	}
 
-	int staffnum = stoi(staff);
+	int staffnum = (int)strtol(staff.c_str(), nullptr, 10);
 	string hairopen = "<";
 	string hairclose = "[";
 	if (form == "dim") {
 		hairopen = ">";
 		hairclose = "]";
 	}
-	double starttime = stod(tstamp) - 1.0;
+	double starttime = strtod(tstamp.c_str(), nullptr) - 1.0;
 	double measure = 0.0;
 	auto loc = tstamp2.find("m+");
 	if (loc != string::npos) {
 		string mnum = tstamp2.substr(0, loc);
-		measure = stod(mnum);
+		measure = strtod(mnum.c_str(), nullptr);
 		tstamp2 = tstamp2.substr(loc+2, string::npos);
 	}
-	double endtime = stod(tstamp2) - 1;
+	double endtime = strtod(tstamp2.c_str(), nullptr) - 1;
 
 	HumNum measurestart = gm->getTimestamp();
 	HumNum timestamp;   // timestamp of event in measure
@@ -101264,7 +101264,7 @@ void Tool_mei2hum::addExtMetaRecords(HumdrumFile& outfile, xml_document& doc) {
 		if (starttimevalue == "") {
 			starttime = 0.0;
 		} else {
-			starttime = stof(starttimevalue);
+			starttime = strtof(starttimevalue.c_str(), nullptr);
 		}
 		if (starttime > 0.0) {
 			continue;
@@ -101290,7 +101290,7 @@ void Tool_mei2hum::addExtMetaRecords(HumdrumFile& outfile, xml_document& doc) {
 		if (starttimevalue == "") {
 			starttime = 0.0;
 		} else {
-			starttime = stof(starttimevalue);
+			starttime = strtof(starttimevalue.c_str(), nullptr);
 		}
 		if (starttime == 0.0) {
 			continue;
@@ -101720,7 +101720,7 @@ void Tool_mei2hum::parseStaffDef(xml_node staffDef, HumNum starttime) {
 		return;
 	}
 
-	int num = stoi(staffnum);
+	int num = (int)strtol(staffnum.c_str(), nullptr, 10);
 	if (num < 1) {
 		// to small
 		return;
@@ -102008,16 +102008,16 @@ void Tool_mei2hum::fillWithStaffDefAttributes(mei_staffDef& staffinfo, xml_node
 		staffinfo.clef = makeHumdrumClef(clefshape, clefline, clefdis, clefdisplace);
 	}
 	if ((!metercount.empty()) && (!meterunit.empty())) {
-		HumNum meterduration = stoi(metercount) * 4 / stoi(meterunit);
+		HumNum meterduration = (int)strtol(metercount.c_str(), nullptr, 10) * 4 / (int)strtol(meterunit.c_str(), nullptr, 10);
 		if (nodename == "scoreDef") {
 			for (int i=0; i<(int)m_measureDuration.size(); i++) {
 				m_measureDuration.at(i) = meterduration;
-				m_currentMeterUnit.at(i) = stoi(meterunit);
+				m_currentMeterUnit.at(i) = (int)strtol(meterunit.c_str(), nullptr, 10);
 			}
 		} else if (nodename == "staffDef") {
 			if (nnum > 0) {
 				m_measureDuration.at(nnum-1) = meterduration;
-				m_currentMeterUnit.at(nnum-1) = stoi(meterunit);
+				m_currentMeterUnit.at(nnum-1) = (int)strtol(meterunit.c_str(), nullptr, 10);
 			}
 		} else {
 			cerr << DKHTP << element.name() << "@meter.count/@meter.unit" << CURRLOC << endl;
@@ -102026,7 +102026,7 @@ void Tool_mei2hum::fillWithStaffDefAttributes(mei_staffDef& staffinfo, xml_node
 		staffinfo.timesig = "*M" + metercount + "/" + meterunit;
 	}
 	if (!keysig.empty()) {
-		int count = stoi(keysig);
+		int count = (int)strtol(keysig.c_str(), nullptr, 10);
 		int accid = 0;
 		if (keysig.find("s") != string::npos) {
 			accid = +1;
@@ -102307,7 +102307,7 @@ HumNum Tool_mei2hum::parseMeasure(xml_node measure, HumNum starttime) {
 	if (n.empty()) {
 		// cerr << "Warning: no measure number on measure element" << endl;
 	} else {
-		nnum = stoi(n);
+		nnum = (int)strtol(n.c_str(), nullptr, 10);
 	}
 	if (nnum < 0) {
 		cerr << "Error: invalid measure number: " << nnum << endl;
@@ -102534,7 +102534,7 @@ HumNum Tool_mei2hum::parseStaff(xml_node staff, HumNum starttime) {
 	if (n.empty()) {
 		cerr << "Warning: no staff number on staff element in measure " << m_currentMeasure << endl;
 	} else {
-		nnum = stoi(n);
+		nnum = (int)strtol(n.c_str(), nullptr, 10);
 	}
 	if (nnum < 1) {
 		cerr << "Error: invalid staff number: " << nnum << endl;
@@ -102615,7 +102615,7 @@ HumNum Tool_mei2hum::parseStaff_mensural(xml_node staff, HumNum starttime) {
 	if (n.empty()) {
 		cerr << "Warning: no staff number on staff element in measure " << m_currentMeasure << endl;
 	} else {
-		nnum = stoi(n);
+		nnum = (int)strtol(n.c_str(), nullptr, 10);
 	}
 	if (nnum < 1) {
 		cerr << "Error: invalid staff number: " << nnum << endl;
@@ -103034,13 +103034,13 @@ HumNum Tool_mei2hum::parseTuplet(xml_node tuplet, HumNum starttime) {
 	if (numbase == "") {
 		cerr << "Warning: tuplet@numbase is empty" << endl;
 	} else {
-		newfactor = stoi(numbase);
+		newfactor = (int)strtol(numbase.c_str(), nullptr, 10);
 	}
 
 	if (num == "") {
 		cerr << "Warning: tuplet@num is empty" << endl;
 	} else {
-		newfactor /= stoi(num);
+		newfactor /= (int)strtol(num.c_str(), nullptr, 10);
 	}
 
 	m_tupletfactor *= newfactor;
@@ -103197,7 +103197,7 @@ bool Tool_mei2hum::beamIsValid(vector<xml_node>& beamlist) {
 			continue;
 		}
 		if (isdigit(dur[0])) {
-			if (stoi(dur) <= 4) {
+			if ((int)strtol(dur.c_str(), nullptr, 10) <= 4) {
 				return false;
 			}
 		} else {
@@ -104089,7 +104089,7 @@ int Tool_mei2hum::getDotCount(xml_node node) {
 	string dots = node.attribute("dots").value();
 	int dotcount = 0;
 	if (dots != "") {
-		dotcount = stoi(dots);
+		dotcount = (int)strtol(dots.c_str(), nullptr, 10);
 	}
 	return dotcount;
 }
@@ -104178,13 +104178,13 @@ void Tool_mei2hum::parseTupletSpanStart(xml_node node,
 	if (numbase == "") {
 		cerr << "Warning: tuplet@numbase is empty" << endl;
 	} else {
-		newfactor = stoi(numbase);
+		newfactor = (int)strtol(numbase.c_str(), nullptr, 10);
 	}
 
 	if (num == "") {
 		cerr << "Warning: tuplet@num is empty" << endl;
 	} else {
-		newfactor /= stoi(num);
+		newfactor /= (int)strtol(num.c_str(), nullptr, 10);
 	}
 
 	m_tupletfactor *= newfactor;
@@ -104221,13 +104221,13 @@ void Tool_mei2hum::parseTupletSpanStop(string& output, xml_node node,
 	if (numbase == "") {
 		cerr << "Warning: tuplet@numbase is empty" << endl;
 	} else {
-		newfactor = stoi(numbase);
+		newfactor = (int)strtol(numbase.c_str(), nullptr, 10);
 	}
 
 	if (num == "") {
 		cerr << "Warning: tuplet@num is empty" << endl;
 	} else {
-		newfactor /= stoi(num);
+		newfactor /= (int)strtol(num.c_str(), nullptr, 10);
 	}
 
 	// undo the tuplet factor:
@@ -104577,7 +104577,7 @@ string Tool_mei2hum::getHumdrumPitch(xml_node note, vector<xml_node>& children)
 	if (oct == "") {
 		cerr << "Empty octave" << endl;
 	} else if (isdigit(oct[0])) {
-		octnum = stoi(oct);
+		octnum = (int)strtol(oct.c_str(), nullptr, 10);
 	} else {
 		cerr << "Unknown octave value: " << oct << endl;
 	}
@@ -104679,7 +104679,7 @@ HumNum Tool_mei2hum::getDuration(xml_node element) {
 		output = 8;
 	} else if (isdigit(dur[0])) {
 		output = 1;
-		output /= stoi(dur);
+		output /= (int)strtol(dur.c_str(), nullptr, 10);
 	} else {
 		cerr << "Unknown " << element.name() << "@dur: " << dur << endl;
 		return 0;
@@ -104694,7 +104694,7 @@ HumNum Tool_mei2hum::getDuration(xml_node element) {
 	if (dots == "") {
 		dotcount = 0;
 	} else if (isdigit(dots[0])) {
-		dotcount = stoi(dots);
+		dotcount = (int)strtol(dots.c_str(), nullptr, 10);
 	} else {
 		cerr << "Unknown " << element.name() << "@dotcount: " << dur << endl;
 		return 0;
@@ -104830,7 +104830,7 @@ void Tool_mei2hum::parseVerse(xml_node verse, GridStaff* staff) {
 	if (n.empty()) {
 		cerr << "Warning: no layer number on layer element" << endl;
 	} else {
-		nnum = stoi(n);
+		nnum = (int)strtol(n.c_str(), nullptr, 10);
 	}
 	if (nnum < 1) {
 		cerr << "Warning: invalid layer number: " << nnum << endl;
@@ -104987,7 +104987,7 @@ string Tool_mei2hum::makeHumdrumClef(const string& shape,
 		const string& line, const string& clefdis, const string& clefdisplace) {
 	string output = "*clef" + shape;
 	if (!clefdis.empty()) {
-		int number = stoi(clefdis);
+		int number = (int)strtol(clefdis.c_str(), nullptr, 10);
 		int count = 0;
 		if (number == 8) {
 			count = 1;
@@ -105239,7 +105239,7 @@ void Tool_mei2hum::parseDir(xml_node dir, HumNum starttime) {
 	}
 
 	double meterunit = m_currentMeterUnit[staffnum - 1];
-	double tsd = (stof(ts)-1) * 4.0 / meterunit;
+	double tsd = (strtof(ts.c_str(), nullptr)-1) * 4.0 / meterunit;
 
 	GridMeasure* gm = m_outdata.back();
 	double tsm = gm->getTimestamp().getFloat();
@@ -105683,7 +105683,7 @@ void Tool_mei2hum::parseHarm(xml_node harm, HumNum starttime) {
 		cerr << "Error: no timestamp on harm element" << endl;
 		return;
 	}
-	double tsd = (stof(ts)-1) * 4.0 / meterunit;
+	double tsd = (strtof(ts.c_str(), nullptr)-1) * 4.0 / meterunit;
 	double tolerance = 0.001;
 	GridMeasure* gm = m_outdata.back();
 	double tsm = gm->getTimestamp().getFloat();
@@ -105797,7 +105797,7 @@ void Tool_mei2hum::parseDynam(xml_node dynam, HumNum starttime) {
 		cerr << "Error: no timestamp on dynam element" << endl;
 		return;
 	}
-	double tsd = (stof(ts)-1) * 4.0 / meterunit;
+	double tsd = (strtof(ts.c_str(), nullptr)-1) * 4.0 / meterunit;
 	double tolerance = 0.001;
 	GridMeasure* gm = m_outdata.back();
 	double tsm = gm->getTimestamp().getFloat();
@@ -111973,7 +111973,7 @@ void Tool_musedata2hum::setMeasureNumber(GridMeasure* gm, MuseRecord& mr) {
 		gm->setMeasureNumber(-1);
 		return;
 	}
-	int num = stoi(line.substr(pos));
+	int num = (int)strtol(line.substr(pos).c_str(), nullptr, 10);
 	if (m_lastbarnum >= 0) {
 		int temp = num;
 		num = m_lastbarnum;
@@ -112061,7 +112061,7 @@ void Tool_musedata2hum::convertLine(GridMeasure* gm, MuseRecord& mr) {
 		}
 
 		if (!attributes["Q"].empty()) {
-			m_quarterDivisions = std::stoi(attributes["Q"]);
+			m_quarterDivisions = (int)strtol(attributes["Q"].c_str(), nullptr, 10);
 		}
 
 		string mclef = attributes["C"];
@@ -114540,7 +114540,7 @@ void Tool_musicxml2hum::addText(GridSlice* slice, GridMeasure* measure, int part
 				defaulty = sibling.attribute("default-y");
 				if (defaulty) {
 					dyQ = true;
-					double number = std::stod(defaulty.value());
+					double number = strtod(defaulty.value(), nullptr);
 					if (number >= 0.0) {
 						placementstring = ":a";
 					} else if (number < 0.0) {
@@ -114867,7 +114867,7 @@ void Tool_musicxml2hum::addTempo(GridSlice* slice, GridMeasure* measure, int par
 
 	if (sound) {
 		string mmtok = "*MM";
-		double mmv = stod(mmvalue);
+		double mmv = strtod(mmvalue.c_str(), nullptr);
 		double mmi = int(mmv + 0.001);
 		if (fabs(mmv - mmi) < 0.01) {
 			stringstream sstream;
@@ -115854,7 +115854,7 @@ int Tool_musicxml2hum::addLyrics(GridStaff* staff, MxmlEvent* event) {
 		string value = child.attribute("number").value();
 		if (hre.search(value, R"(verse(\d+))")) {
 			// Fix for Sibelius which uses number="part8verse5" format.
-			number = stoi(hre.getMatch(1));
+			number = (int)strtol(hre.getMatch(1).c_str(), nullptr, 10);
 		} else {
 			number = atoi(child.attribute("number").value());
 		}
@@ -116438,7 +116438,7 @@ void Tool_musicxml2hum::addStriaLine(GridMeasure* outdata,
 		for (int j=0; j<(int)stafflines[i].size(); j++) {
 			if (stafflines[i][j]) {
 				string lines = stafflines[i][j].child_value();
-				int linecount = stoi(lines);
+				int linecount = (int)strtol(lines.c_str(), nullptr, 10);
 				insertPartStria(linecount, *slice->at(i));
 			}
 		}
@@ -118338,7 +118338,7 @@ void Tool_myank::getMarkString(ostream& out, HumdrumFile& infile)  {
 	for (int i=0; i<infile.getLineCount(); i++) {
 		if (infile[i].isBarline()) {
 			if (hre.search(infile.token(i, 0), "^=.*?(\\d+)", "")) {
-				curmeasure = stoi(hre.getMatch(1));
+				curmeasure = (int)strtol(hre.getMatch(1).c_str(), nullptr, 10);
 				hasmark = 0;
 			}
 		}
@@ -119665,7 +119665,7 @@ void Tool_myank::getMeasureStartStop(vector<MeasureInfo>& measurelist, HumdrumFi
 		//if (!hre.search(infile.token(i, 0), "^=.*(\\d+)")) {
 		//   continue;
 		//}
-		//barnum1 = stoi(hre.getMatch(1));
+		//barnum1 = (int)strtol(hre.getMatch(1).c_str(), nullptr, 10);
 		if (!sscanf(infile.token(i, 0)->c_str(), "=%d", &barnum1)) {
 			continue;
 		}
@@ -119677,7 +119677,7 @@ void Tool_myank::getMeasureStartStop(vector<MeasureInfo>& measurelist, HumdrumFi
 				continue;
 			}
 			//if (hre.search(infile.token(ii, 0), "^=.*(\\d+)")) {
-			//   barnum2 = stoi(hre.getMatch(1));
+			//   barnum2 = (int)strtol(hre.getMatch(1).c_str(), nullptr, 10);
 			//   current.stop = ii;
 			//   lastend = ii;
 			//   i = ii - 1;
@@ -119686,7 +119686,7 @@ void Tool_myank::getMeasureStartStop(vector<MeasureInfo>& measurelist, HumdrumFi
 			//}
 			if (hre.search(infile.token(ii, 0), "=[^\\d]*(\\d+)")) {
 			// if (sscanf(infile.token(ii, 0), "=%d", &barnum2)) {
-				barnum2 = stoi(hre.getMatch(1));
+				barnum2 = (int)strtol(hre.getMatch(1).c_str(), nullptr, 10);
 				current.stop = ii;
 				lastend = ii;
 				i = ii - 1;
@@ -128008,7 +128008,7 @@ string Tool_rphrase::getCompositeLabel(HumdrumFile& infile) {
 	HumRegex hre;
 
 	if (hre.search(voices, "^\\d+$")) {
-		int vint = stoi(voices);
+		int vint = (int)strtol(voices.c_str(), nullptr, 10);
 		if (vint != (int)kstarts.size()) {
 			output += "(";
 			output += to_string(kstarts.size());
@@ -130494,7 +130494,7 @@ int Tool_semitones::processKernSpines(HumdrumFile& infile, int line, int start,
             if (!isdigit(intervals[i].back())) {
 					continue;
 				}
-				int value = stoi(intervals[i]);
+				int value = (int)strtol(intervals[i].c_str(), nullptr, 10);
 				if (m_upQ && m_stepQ && (value > 0) && (value < m_leap)) {
 					markInterval(toks[i]);
 				} else if (m_downQ && m_stepQ && (value < 0) && (value > -m_leap)) {
diff --git a/src/ioabc.cpp b/src/ioabc.cpp
index 7f497b5b7..a4800a596 100644
--- a/src/ioabc.cpp
+++ b/src/ioabc.cpp
@@ -1101,7 +1101,7 @@ void ABCInput::ParseLyrics()
             sylType = sylLog_CON_s;
         }
         else if (abcLine.at(found) == '-') {
-            if (abcLine.at(found - 1) == '\\') {
+            if (found > 0 && abcLine.at(found - 1) == '\\') {
                 counter = 0;
             }
             sylType = sylLog_CON_d;
@@ -1146,7 +1146,7 @@ void ABCInput::ParseLyrics()
     // Iterate over notes and syllables simultaneously. Move through note array using counters for each syllable, moving
     // for several notes if syllable needs to be held
     for (int i = 0, j = 0; (i < (int)m_lineNoteArray.size()) && (j < (int)syllables.size()); ++j) {
-        while (m_lineNoteArray.at(i)->IsGraceNote() && (i < (int)m_lineNoteArray.size())) {
+        while ((i < (int)m_lineNoteArray.size()) && m_lineNoteArray.at(i)->IsGraceNote()) {
             ++i;
         }
         if (i >= (int)m_lineNoteArray.size()) break;
@@ -1266,7 +1266,7 @@ void ABCInput::ReadMusicCode(const std::string &musicCode, Section *section)
             ++i;
             ++i;
             std::string information;
-            while (musicCode.at(i) != ']') {
+            while (i < (int)musicCode.length() && musicCode.at(i) != ']') {
                 information.push_back(musicCode.at(i));
                 ++i;
             }
@@ -1295,7 +1295,7 @@ void ABCInput::ReadMusicCode(const std::string &musicCode, Section *section)
             ++i;
             if (!isspace(musicCode.at(i))) {
                 std::string decorationString;
-                while (musicCode.at(i) != m_decoration) {
+                while (i < (int)musicCode.length() && musicCode.at(i) != m_decoration) {
                     decorationString.push_back(musicCode.at(i));
                     ++i;
                 }
@@ -1734,7 +1734,7 @@ void ABCInput::ReadMusicCode(const std::string &musicCode, Section *section)
                 ++i;
             }
             std::string chordSymbol;
-            while (musicCode.at(i) != '\"') {
+            while (i < (int)musicCode.length() && musicCode.at(i) != '\"') {
                 chordSymbol.push_back(musicCode.at(i));
                 ++i;
             }
diff --git a/src/iohumdrum.cpp b/src/iohumdrum.cpp
index 8ca7bfa2c..0979f0b3b 100644
--- a/src/iohumdrum.cpp
+++ b/src/iohumdrum.cpp
@@ -3176,7 +3176,7 @@ DateWithErrors HumdrumInput::dateWithErrorsFromHumdrumDate(const std::string &hu
                     date.valid = false;
                     return date;
                 }
-                values[i] = stoi(value);
+                values[i] = (int)strtol(value.c_str(), nullptr, 10);
             }
         }
         catch (...) {
@@ -7360,7 +7360,7 @@ int HumdrumInput::getStaffNumberLabel(hum::HTp spinestart)
             tok = tok->getNextToken();
             continue;
         }
-        return stoi(number);
+        return (int)strtol(number.c_str(), nullptr, 10);
     }
     return 0;
 }
@@ -7394,7 +7394,7 @@ int HumdrumInput::getPartNumberLabel(hum::HTp spinestart)
             tok = tok->getNextToken();
             continue;
         }
-        return stoi(number);
+        return (int)strtol(number.c_str(), nullptr, 10);
     }
     return 0;
 }
@@ -7428,7 +7428,7 @@ int HumdrumInput::getGroupNumberLabel(hum::HTp spinestart)
             tok = tok->getNextToken();
             continue;
         }
-        return stoi(number);
+        return (int)strtol(number.c_str(), nullptr, 10);
     }
     return 0;
 }
@@ -7490,7 +7490,7 @@ void HumdrumInput::addMidiTempo(ScoreDef *scoreDef, hum::HTp kernpart, int top,
         if (kernpart->compare(0, 3, "*MM") == 0) {
             if (kernpart->size() > 3) {
                 if (::isdigit((*kernpart)[3])) {
-                    int tempo = stoi(kernpart->substr(3));
+                    int tempo = (int)strtol(kernpart->substr(3).c_str(), nullptr, 10);
                     // std::string tempostr = to_string(tempo);
                     scoreDef->SetMidiBpm(tempo * m_globalTempoScaling * m_localTempoScaling.getFloat());
                     foundtempo = true;
@@ -8926,16 +8926,16 @@ void HumdrumInput::setMensurationSymbol(
         std::string num4 = hre.getMatch(4);
 
         if (!num1.empty()) {
-            maximodus = stoi(num1);
+            maximodus = (int)strtol(num1.c_str(), nullptr, 10);
         }
         if (!num2.empty()) {
-            modus = stoi(num2);
+            modus = (int)strtol(num2.c_str(), nullptr, 10);
         }
         if (!num3.empty()) {
-            tempus = stoi(num3);
+            tempus = (int)strtol(num3.c_str(), nullptr, 10);
         }
         if (!num4.empty()) {
-            prolatio = stoi(num4);
+            prolatio = (int)strtol(num4.c_str(), nullptr, 10);
         }
 
         std::stringstream warning;
@@ -9133,10 +9133,10 @@ void HumdrumInput::setTimeSig(ELEMENT element, hum::HTp timesigtok, hum::HTp met
     int count = -1;
     int unit = -1;
     if (timesigtok && regex_search(*timesigtok, matches, regex("^\\*M(\\d+)/(\\d+)%(\\d+)"))) {
-        count = stoi(matches[1]);
-        // int top = stoi(matches[1]);
-        int bot = stoi(matches[2]);
-        int bot2 = stoi(matches[3]);
+        count = (int)strtol(matches[1].str().c_str(), nullptr, 10);
+        // int top = (int)strtol(matches[1].str().c_str(), nullptr, 10);
+        int bot = (int)strtol(matches[2].str().c_str(), nullptr, 10);
+        int bot2 = (int)strtol(matches[3].str().c_str(), nullptr, 10);
         if ((metersig == "3") && (bot == 3) && (bot2 == 2)) {
             MeterSig *vrvmetersig = getMeterSig(element);
             vrvmetersig->SetCount({ { 3 }, MeterCountSign::None });
@@ -9146,8 +9146,8 @@ void HumdrumInput::setTimeSig(ELEMENT element, hum::HTp timesigtok, hum::HTp met
         }
     }
     else if (timesigtok && regex_search(*timesigtok, matches, regex("^\\*M(\\d+)/(\\d+)"))) {
-        count = stoi(matches[1]);
-        unit = stoi(matches[2]);
+        count = (int)strtol(matches[1].str().c_str(), nullptr, 10);
+        unit = (int)strtol(matches[2].str().c_str(), nullptr, 10);
         if (unit == 0) {
             // Breve time signature not possible in MEI, so change to whole note unit:
             count *= 2;
@@ -9160,9 +9160,9 @@ void HumdrumInput::setTimeSig(ELEMENT element, hum::HTp timesigtok, hum::HTp met
             checkMeterSigParameters(vrvmetersig, timesigtok);
         }
         else if (*metersigtok == "*met()") {
-            unit = stoi(matches[2]);
+            unit = (int)strtol(matches[2].str().c_str(), nullptr, 10);
             MeterSig *vrvmetersig = getMeterSig(element);
-            vrvmetersig->SetCount({ { std::stoi(matches[1]) }, MeterCountSign::None });
+            vrvmetersig->SetCount({ { (int)strtol(matches[1].str().c_str(), nullptr, 10) }, MeterCountSign::None });
             vrvmetersig->SetUnit(unit);
             vrvmetersig->SetVisible(BOOLEAN_false);
         }
@@ -9182,9 +9182,9 @@ void HumdrumInput::setTimeSig(ELEMENT element, hum::HTp timesigtok, hum::HTp met
             && (metersigtok->find('O') == std::string::npos)) {
             // Only storing the time signature if there is no mensuration
             // otherwise verovio will display both.
-            unit = stoi(matches[2]);
+            unit = (int)strtol(matches[2].str().c_str(), nullptr, 10);
             MeterSig *vrvmetersig = getMeterSig(element);
-            vrvmetersig->SetCount({ { std::stoi(matches[1]) }, MeterCountSign::None });
+            vrvmetersig->SetCount({ { (int)strtol(matches[1].str().c_str(), nullptr, 10) }, MeterCountSign::None });
             vrvmetersig->SetUnit(unit);
         }
         else {
@@ -9192,7 +9192,7 @@ void HumdrumInput::setTimeSig(ELEMENT element, hum::HTp timesigtok, hum::HTp met
             // are in reference to it (can't add meter.count since
             // this will also print a time signature.
             // Count now allowed (suppress time signature display with @form="invis").
-            unit = stoi(matches[2]);
+            unit = (int)strtol(matches[2].str().c_str(), nullptr, 10);
             if (unit == 0) {
                 count *= 2;
                 unit = 1;
@@ -17826,12 +17826,12 @@ void HumdrumInput::processLinkedDirection(int index, hum::HTp token, int staffin
         }
         if (key == "vgrp") {
             if ((!value.empty()) && std::isdigit(value[0])) {
-                vgroup = std::stoi(value);
+                vgroup = (int)strtol(value.c_str(), nullptr, 10);
             }
         }
         else if (key == "vg") {
             if ((!value.empty()) && std::isdigit(value[0])) {
-                vgroup = std::stoi(value);
+                vgroup = (int)strtol(value.c_str(), nullptr, 10);
             }
         }
     }
@@ -19187,7 +19187,7 @@ void HumdrumInput::processDynamics(hum::HTp token, int staffindex)
                         hairpin->SetVgrp(VGRP_DYNAM_DEFAULT);
                     }
                     else if (std::isdigit(verticalgroup[0])) {
-                        hairpin->SetVgrp(stoi(verticalgroup));
+                        hairpin->SetVgrp((int)strtol(verticalgroup.c_str(), nullptr, 10));
                     }
                     else {
                         // don't set a vertical group for this token
@@ -19336,7 +19336,7 @@ void HumdrumInput::processDynamics(hum::HTp token, int staffindex)
                     hairpin->SetVgrp(VGRP_DYNAM_DEFAULT);
                 }
                 else if (std::isdigit(verticalgroup[0])) {
-                    hairpin->SetVgrp(stoi(verticalgroup));
+                    hairpin->SetVgrp((int)strtol(verticalgroup.c_str(), nullptr, 10));
                 }
                 else {
                     // don't set a vertical group for this token
@@ -19691,7 +19691,7 @@ void HumdrumInput::addDynamicsMark(hum::HTp dyntok, hum::HTp token, hum::HLp lin
                 dynam->SetVgrp(VGRP_DYNAM_DEFAULT);
             }
             else if (std::isdigit(verticalgroup[0])) {
-                dynam->SetVgrp(stoi(verticalgroup));
+                dynam->SetVgrp((int)strtol(verticalgroup.c_str(), nullptr, 10));
             }
             else {
                 // don't set a vertical group for this token
@@ -20128,7 +20128,7 @@ bool HumdrumInput::hasAboveParameter(hum::HTp token, const std::string &category
                 else if (!value.empty()) {
                     // b=2 means below the staff below the attached staff.
                     if (isdigit(value[0])) {
-                        output = stoi(value);
+                        output = (int)strtol(value.c_str(), nullptr, 10);
                         if (output) {
                             output = -(output - 1);
                         }
@@ -20178,7 +20178,7 @@ bool HumdrumInput::hasCenterParameter(hum::HTp token, const std::string &categor
                 else if (!value.empty()) {
                     // c=2 means below the staff below the attached staff.
                     if (isdigit(value[0])) {
-                        output = stoi(value);
+                        output = (int)strtol(value.c_str(), nullptr, 10);
                         if (output) {
                             output = -(output - 1);
                         }
@@ -20342,7 +20342,7 @@ bool HumdrumInput::hasBelowParameter(hum::HTp token, const std::string &category
                 else if (!value.empty()) {
                     // b=2 means below the staff below the attached staff.
                     if (isdigit(value[0])) {
-                        output = stoi(value);
+                        output = (int)strtol(value.c_str(), nullptr, 10);
                         if (output) {
                             output = -(output - 1);
                         }
@@ -21783,11 +21783,11 @@ void HumdrumInput::insertMeterSigElement(
     int count = -1;
     int unit = -1;
     if (regex_search(*tsig, matches, regex("^\\*M(\\d+)/(\\d+)"))) {
-        count = stoi(matches[1]);
-        unit = stoi(matches[2]);
+        count = (int)strtol(matches[1].str().c_str(), nullptr, 10);
+        unit = (int)strtol(matches[2].str().c_str(), nullptr, 10);
     }
     else if (regex_search(*tsig, matches, regex("^\\*M(\\d+)"))) {
-        count = stoi(matches[1]);
+        count = (int)strtol(matches[1].str().c_str(), nullptr, 10);
     }
     // deal with non-rational units here.
     if (count < 0) {
@@ -23897,7 +23897,7 @@ void HumdrumInput::assignScalingToTupletGroup(std::vector<humaux::HumdrumBeamAnd
     // Set a specific number for the tuplet (which make sense).
     std::string num = tggroup[0]->token->getLayoutParameter("TUP", "num");
     if (!num.empty()) {
-        int numvalue = stoi(num);
+        int numvalue = (int)strtol(num.c_str(), nullptr, 10);
         if (numvalue > 0) {
             hum::HumNum scale = num;
             scale /= tggroup[0]->num;
@@ -25317,7 +25317,7 @@ void HumdrumInput::assignVerticalGroup(Pedal *pedal, hum::HTp token)
         pedal->SetVgrp(VGRP_PEDAL_DEFAULT);
     }
     else if (std::isdigit(vg[0])) {
-        int value = stoi(vg);
+        int value = (int)strtol(vg.c_str(), nullptr, 10);
         if (value < 0) {
             // do not assign a vertical group
         }
@@ -25906,7 +25906,7 @@ void HumdrumInput::convertMRest(MRest *rest, hum::HTp token, int subtoken, int s
     int ottava = token->getValueInt("auto", "ottava");
 
     if ((!oloc.empty()) && (!ploc.empty())) {
-        int olocint = stoi(oloc);
+        int olocint = (int)strtol(oloc.c_str(), nullptr, 10);
         olocint -= ottava;
         rest->SetOloc(olocint);
         if (ploc == "C") {
@@ -25979,7 +25979,7 @@ void HumdrumInput::convertRest(Rest *rest, hum::HTp token, int subtoken, int sta
     if (percussionQ) {
         if ((!oloc.empty()) && (!ploc.empty())) {
             // treat as treble clef, but convert to @loc
-            int olocint = stoi(oloc);
+            int olocint = (int)strtol(oloc.c_str(), nullptr, 10);
             olocint -= ottava;
             int plocint = 0;
             if (ploc == "C") {
@@ -26010,7 +26010,7 @@ void HumdrumInput::convertRest(Rest *rest, hum::HTp token, int subtoken, int sta
     }
     else {
         if ((!oloc.empty()) && (!ploc.empty())) {
-            int olocint = stoi(oloc);
+            int olocint = (int)strtol(oloc.c_str(), nullptr, 10);
             olocint -= ottava;
             rest->SetOloc(olocint);
             if (ploc == "C") {
@@ -26118,7 +26118,7 @@ void HumdrumInput::setStemLength(Note *note, hum::HTp token)
     if (stemlen.empty()) {
         return;
     }
-    double value = std::stof(stemlen);
+    double value = strtof(stemlen.c_str(), nullptr);
     note->SetStemLen(value);
 }
 
@@ -29282,7 +29282,7 @@ void HumdrumInput::addMordent(Object *linked, hum::HTp token)
             bool hasaccid = accid.empty() ? false : true;
             int accidval = 0;
             if (hasaccid) {
-                accidval = stoi(accid);
+                accidval = (int)strtol(accid.c_str(), nullptr, 10);
                 switch (accidval) {
                     case -1: mordent->SetAccidlower(ACCIDENTAL_WRITTEN_f); break;
                     case 0: mordent->SetAccidlower(ACCIDENTAL_WRITTEN_n); break;
@@ -29298,7 +29298,7 @@ void HumdrumInput::addMordent(Object *linked, hum::HTp token)
             bool hasaccid = accid.empty() ? false : true;
             int accidval = 0;
             if (hasaccid) {
-                accidval = stoi(accid);
+                accidval = (int)strtol(accid.c_str(), nullptr, 10);
                 switch (accidval) {
                     case -1: mordent->SetAccidupper(ACCIDENTAL_WRITTEN_f); break;
                     case 0: mordent->SetAccidupper(ACCIDENTAL_WRITTEN_n); break;
@@ -29446,7 +29446,7 @@ void HumdrumInput::addTrill(Object *linked, hum::HTp token)
     bool hasaccid = accid.empty() ? false : true;
     int accidval = 0;
     if (hasaccid) {
-        accidval = stoi(accid);
+        accidval = (int)strtol(accid.c_str(), nullptr, 10);
         switch (accidval) {
             case -1: trill->SetAccidupper(ACCIDENTAL_WRITTEN_f); break;
             case 0: trill->SetAccidupper(ACCIDENTAL_WRITTEN_n); break;
@@ -30162,7 +30162,7 @@ void HumdrumInput::setupSystemMeasure(int startline, int endline)
         if (ending) {
             std::smatch matches;
             if (regex_search(currentsection, matches, regex("(\\d+)$"))) {
-                endnum = stoi(matches[1]);
+                endnum = (int)strtol(matches[1].str().c_str(), nullptr, 10);
             }
             else {
                 endnum = 0;
@@ -32204,7 +32204,7 @@ void HumdrumInput::prepareSections()
 
         //        std::smatch matches;
         //        if (regex_search(*((string *)infile.token(i, 0)), matches, regex("(\\d+)$"))) {
-        //            endnum = stoi(matches[1]);
+        //            endnum = (int)strtol(matches[1].str().c_str(), nullptr, 10);
         //            ending[i] = endnum;
         //        }
         //        else {
diff --git a/src/iomei.cpp b/src/iomei.cpp
index 90551d2a5..244df446d 100644
--- a/src/iomei.cpp
+++ b/src/iomei.cpp
@@ -1589,7 +1589,7 @@ void MEIOutput::WriteRevisionDesc(pugi::xml_node meiHead)
         value = chromatic;
     }
     else if (transposer.IsValidSemitones(transposition)) {
-        value = stoi(transposition);
+        value = (int)strtol(transposition.c_str(), nullptr, 10);
     }
     else if (transposer.IsValidKeyTonic(transposition)) {
         // Prepare message for the KeyTonic transposition
@@ -8847,7 +8847,7 @@ void MEIInput::UpgradeDurGesTo_4_0_0(pugi::xml_node element, DurationInterface *
     if (element.attribute("dur.ges")) {
         std::string durGes = element.attribute("dur.ges").as_string();
         if (durGes.back() == 'p') {
-            interface->SetDurPpq(std::stoi(durGes));
+            interface->SetDurPpq((int)strtol(durGes.c_str(), nullptr, 10));
         }
         else if (durGes.back() == 'r') {
             durGes.pop_back();
@@ -8855,11 +8855,13 @@ void MEIInput::UpgradeDurGesTo_4_0_0(pugi::xml_node element, DurationInterface *
         }
         else if (durGes.back() == 's') {
             durGes.pop_back();
-            try {
-                interface->SetDurReal(std::stof(durGes));
+            char *end = nullptr;
+            float val = std::strtof(durGes.c_str(), &end);
+            if (end != durGes.c_str()) {
+                interface->SetDurReal(val);
             }
-            catch (const std::invalid_argument &ia) {
-                LogError("Upgrading to 4.0.0: %s", ia.what());
+            else {
+                LogError("Upgrading to 4.0.0: invalid float '%s'", durGes.c_str());
             }
         }
         element.remove_attribute("dur.ges");
diff --git a/src/iomusxml.cpp b/src/iomusxml.cpp
index 0192c599b..ef8299039 100644
--- a/src/iomusxml.cpp
+++ b/src/iomusxml.cpp
@@ -287,7 +287,7 @@ void MusicXmlInput::InsertClefToLayer(Staff *staff, Layer *layer, Clef *clef, in
         Layer *otherLayer = vrv_cast<Layer *>(listLayer);
         if (m_layerTimes.find(otherLayer) == m_layerTimes.end()) continue;
         // Get first element for the same (or higher if same is not present) duration
-        const auto &start = m_layerTimes.at(otherLayer).lower_bound(scoreOnset);
+        const auto &start = m_layerTimes[otherLayer].lower_bound(scoreOnset);
         // Add either clef or #sameas, depending on the layer we're adding to
         Clef *clefToAdd = NULL;
         if (listLayer == layer) {
@@ -305,14 +305,14 @@ void MusicXmlInput::InsertClefToLayer(Staff *staff, Layer *layer, Clef *clef, in
         else {
             // If corresponding time couldn't be found (i.e. it's higher than any other duration in the layer) - add
             // clef to the end of the layer
-            if (start == m_layerTimes.at(otherLayer).end()) {
+            if (start == m_layerTimes[otherLayer].end()) {
                 otherLayer->AddChild(clefToAdd);
-                m_layerTimes.at(otherLayer).emplace(std::prev(m_layerTimes.at(otherLayer).end())->first, clefToAdd);
+                m_layerTimes[otherLayer].emplace(std::prev(m_layerTimes[otherLayer].end())->first, clefToAdd);
             }
             else {
                 // Always try to add clefs at the end of current duration, to honor their order in the musicxml
                 const int actualScoreOnSet = start->first;
-                auto end = m_layerTimes.at(otherLayer).upper_bound(actualScoreOnSet);
+                auto end = m_layerTimes[otherLayer].upper_bound(actualScoreOnSet);
                 LayerElement *layerElement = (--end)->second;
                 this->InsertClefIntoObject(layerElement, clefToAdd, otherLayer, scoreOnset, true);
             }
@@ -325,7 +325,7 @@ void MusicXmlInput::InsertClefIntoObject(
 {
     if (layerElement->GetParent()->Is(LAYER)) {
         this->InsertClefIntoObject(layer, clef, layerElement, insertAfter);
-        m_layerTimes.at(layer).emplace(scoreOnset, clef);
+        m_layerTimes[layer].emplace(scoreOnset, clef);
     }
     else {
         Object *parent = layerElement->GetParent();
@@ -375,7 +375,7 @@ void MusicXmlInput::AddMeasure(Section *section, Measure *measure, int i)
         // Prefer any measure with matching index (measure numbers might be non-unique)
         for (Object *object : matchingMeasures) {
             Measure *matchingMeasure = vrv_cast<Measure *>(object);
-            if (m_measureCounts.at(matchingMeasure) == i) {
+            if (m_measureCounts[matchingMeasure] == i) {
                 existingMeasure = matchingMeasure;
                 break;
             }
@@ -439,16 +439,16 @@ void MusicXmlInput::AddLayerElement(Layer *layer, LayerElement *element, int dur
     assert(element);
 
     int currTime = 0;
-    if (m_layerEndTimes.contains(layer)) currTime = m_layerEndTimes.at(layer);
+    if (m_layerEndTimes.contains(layer)) currTime = m_layerEndTimes[layer];
     if ((layer->GetChildren().size() == 0 && m_durTotal > 0) || currTime < m_durTotal) {
         this->FillSpace(layer, m_durTotal - currTime, true);
     }
 
-    if (m_elementStackMap.at(layer).empty()) {
+    if (m_elementStackMap[layer].empty()) {
         layer->AddChild(element);
     }
     else {
-        m_elementStackMap.at(layer).back()->AddChild(element);
+        m_elementStackMap[layer].back()->AddChild(element);
     }
     // Recheck if AddChild was successful
     if (!element->GetParent()) return;
@@ -533,9 +533,9 @@ Layer *MusicXmlInput::SelectLayer(short int layerNum, Staff *staff)
 void MusicXmlInput::RemoveLastFromStack(ClassId classId, Layer *layer)
 {
     std::vector<LayerElement *>::reverse_iterator riter;
-    for (riter = m_elementStackMap.at(layer).rbegin(); riter != m_elementStackMap.at(layer).rend(); ++riter) {
+    for (riter = m_elementStackMap[layer].rbegin(); riter != m_elementStackMap[layer].rend(); ++riter) {
         if ((*riter)->Is(classId)) {
-            m_elementStackMap.at(layer).erase((riter + 1).base());
+            m_elementStackMap[layer].erase((riter + 1).base());
             return;
         }
     }
@@ -543,8 +543,8 @@ void MusicXmlInput::RemoveLastFromStack(ClassId classId, Layer *layer)
 
 bool MusicXmlInput::IsInStack(ClassId classId, Layer *layer)
 {
-    return (m_elementStackMap.at(layer).end()
-        != std::find_if(m_elementStackMap.at(layer).begin(), m_elementStackMap.at(layer).end(),
+    return (m_elementStackMap[layer].end()
+        != std::find_if(m_elementStackMap[layer].begin(), m_elementStackMap[layer].end(),
             [classId](LayerElement *element) { return element->Is(classId); }));
 }
 
@@ -588,11 +588,11 @@ void MusicXmlInput::FillSpace(Layer *layer, int dur, bool withClefs, int offset)
         Space *space = new Space();
         space->SetDur(space->AttDurationLog::StrToDuration(durStr));
         space->SetDurPpq(m_ppq * quarters);
-        if (m_elementStackMap.at(layer).empty()) {
+        if (m_elementStackMap[layer].empty()) {
             layer->AddChild(space);
         }
         else {
-            m_elementStackMap.at(layer).back()->AddChild(space);
+            m_elementStackMap[layer].back()->AddChild(space);
         }
         dur -= m_ppq * quarters;
         offset += m_ppq * quarters;
@@ -602,7 +602,8 @@ void MusicXmlInput::FillSpace(Layer *layer, int dur, bool withClefs, int offset)
 
 void MusicXmlInput::GenerateID(pugi::xml_node node)
 {
-    std::string id = std::string(node.name()).at(0) + Object::GenerateHashID();
+    std::string name = node.name();
+    std::string id = (name.empty() ? std::string("x") : std::string(1, name[0])) + Object::GenerateHashID();
     node.append_attribute("xml:id").set_value(id.c_str());
 }
 
@@ -856,7 +857,7 @@ void MusicXmlInput::PrintMetronome(pugi::xml_node metronome, Tempo *tempo)
                 std::string matches("0123456789");
                 std::size_t offset = iter->second.find_first_of(matches);
                 if (offset < iter->second.length()) {
-                    const double mmval = std::stod(iter->second.substr(offset));
+                    const double mmval = strtod(iter->second.substr(offset).c_str(), nullptr);
                     tempo->SetMm(mmval);
                 }
                 if (!iter->second.empty()) {
@@ -2273,7 +2274,7 @@ void MusicXmlInput::ReadMusicXmlDirection(
             }
             else {
                 const int measureDifference
-                    = m_measureCounts.at(measure) - m_bracketStack.front().second.m_lastMeasureCount;
+                    = m_measureCounts[measure] - m_bracketStack.front().second.m_lastMeasureCount;
                 m_bracketStack.front().first->SetLendsym(
                     this->ConvertLineEndSymbol(bracket.attribute("line-end").as_string()));
                 if (measureDifference >= 0) {
@@ -2284,7 +2285,7 @@ void MusicXmlInput::ReadMusicXmlDirection(
         }
         else {
             BracketSpan *bracketSpan = new BracketSpan();
-            musicxml::OpenSpanner openBracket(voiceNumber, m_measureCounts.at(measure));
+            musicxml::OpenSpanner openBracket(voiceNumber, m_measureCounts[measure]);
             bracketSpan->SetColor(bracket.attribute("color").as_string());
             bracketSpan->SetLform(
                 bracketSpan->AttLineRendBase::StrToLineform(bracket.attribute("line-type").as_string()));
@@ -2308,7 +2309,7 @@ void MusicXmlInput::ReadMusicXmlDirection(
             std::vector<std::pair<ControlElement *, musicxml::OpenDashes>>::iterator iter = m_openDashesStack.begin();
             while (iter != m_openDashesStack.end()) {
                 if (iter->second.m_dirN == dashesNumber && iter->second.m_staffNum == staffNum) {
-                    const int measureDifference = m_measureCounts.at(measure) - iter->second.m_measureCount;
+                    const int measureDifference = m_measureCounts[measure] - iter->second.m_measureCount;
                     if (measureDifference >= 0) {
                         if (iter->first->Is(DYNAM))
                             dynamic_cast<Dynam *>(iter->first)
@@ -2354,7 +2355,7 @@ void MusicXmlInput::ReadMusicXmlDirection(
                 }
             }
             if (controlElement) {
-                musicxml::OpenDashes openDashes(dashesNumber, staffNum, m_measureCounts.at(measure));
+                musicxml::OpenDashes openDashes(dashesNumber, staffNum, m_measureCounts[measure]);
                 m_openDashesStack.push_back({ controlElement, openDashes });
             }
             else {
@@ -2423,7 +2424,7 @@ void MusicXmlInput::ReadMusicXmlDirection(
                 else {
                     dir->SetLform(LINEFORM_dashed);
                 }
-                musicxml::OpenDashes openDashes(extNumber, staffNum, m_measureCounts.at(measure));
+                musicxml::OpenDashes openDashes(extNumber, staffNum, m_measureCounts[measure]);
                 m_openDashesStack.push_back({ dir, openDashes });
             }
         }
@@ -2500,7 +2501,7 @@ void MusicXmlInput::ReadMusicXmlDirection(
                 else {
                     dynam->SetLform(LINEFORM_dashed);
                 }
-                musicxml::OpenDashes openDashes(extNumber, staffNum, m_measureCounts.at(measure));
+                musicxml::OpenDashes openDashes(extNumber, staffNum, m_measureCounts[measure]);
                 m_openDashesStack.push_back({ dynam, openDashes });
             }
         }
@@ -2517,7 +2518,7 @@ void MusicXmlInput::ReadMusicXmlDirection(
             std::vector<std::pair<Hairpin *, musicxml::OpenSpanner>>::iterator iter;
             for (iter = m_hairpinStack.begin(); iter != m_hairpinStack.end(); ++iter) {
                 if (iter->second.m_dirN == hairpinNumber) {
-                    const int measureDifference = m_measureCounts.at(measure) - iter->second.m_lastMeasureCount;
+                    const int measureDifference = m_measureCounts[measure] - iter->second.m_lastMeasureCount;
                     if (measureDifference >= 0) {
                         iter->first->SetTstamp2(std::pair<int, double>(measureDifference, timeStamp));
                     }
@@ -2538,12 +2539,12 @@ void MusicXmlInput::ReadMusicXmlDirection(
             }
             if (!matchedWedge) {
                 m_hairpinStopStack.push_back(std::tuple<int, double, musicxml::OpenSpanner>(
-                    0, timeStamp, musicxml::OpenSpanner(hairpinNumber, m_measureCounts.at(measure))));
+                    0, timeStamp, musicxml::OpenSpanner(hairpinNumber, m_measureCounts[measure])));
             }
         }
         else {
             Hairpin *hairpin = new Hairpin();
-            musicxml::OpenSpanner openHairpin(hairpinNumber, m_measureCounts.at(measure));
+            musicxml::OpenSpanner openHairpin(hairpinNumber, m_measureCounts[measure]);
             if (HasAttributeWithValue(wedge->node(), "type", "crescendo")) {
                 hairpin->SetForm(hairpinLog_FORM_cres);
             }
@@ -2586,7 +2587,7 @@ void MusicXmlInput::ReadMusicXmlDirection(
             hairpin->SetVgrp(defaultY);
             // match new hairpin to existing hairpin stop
             for (auto iter = m_hairpinStopStack.begin(); iter != m_hairpinStopStack.end(); ++iter) {
-                const int measureDifference = std::get<2>(*iter).m_lastMeasureCount - m_measureCounts.at(measure);
+                const int measureDifference = std::get<2>(*iter).m_lastMeasureCount - m_measureCounts[measure];
                 if ((std::get<2>(*iter).m_dirN == hairpinNumber) && (measureDifference == 0)) {
                     if (measureDifference >= 0) {
                         hairpin->SetTstamp2(std::pair<int, double>(measureDifference, std::get<1>(*iter)));
@@ -2700,7 +2701,7 @@ void MusicXmlInput::ReadMusicXmlDirection(
         voiceNumber = (voiceNumber < 1) ? 1 : voiceNumber;
         if (HasAttributeWithValue(lead, "type", "stop")) {
             const int measureDifference
-                = m_measureCounts.at(measure) - m_bracketStack.front().second.m_lastMeasureCount;
+                = m_measureCounts[measure] - m_bracketStack.front().second.m_lastMeasureCount;
             if (measureDifference >= 0) {
                 m_bracketStack.front().first->SetTstamp2(std::pair<int, double>(measureDifference, timeStamp));
             }
@@ -2709,7 +2710,7 @@ void MusicXmlInput::ReadMusicXmlDirection(
         else {
             // std::string symbol = lead.node().attribute("symbol").as_string();
             BracketSpan *bracketSpan = new BracketSpan();
-            musicxml::OpenSpanner openBracket(voiceNumber, m_measureCounts.at(measure));
+            musicxml::OpenSpanner openBracket(voiceNumber, m_measureCounts[measure]);
             bracketSpan->SetColor(lead.attribute("color").as_string());
             // bracketSpan->SetPlace(bracketSpan->AttPlacementRelStaff::StrToStaffrel(placeStr.c_str()));
             bracketSpan->SetFunc(bracketSpanLog_FUNC_analytical);
@@ -2975,7 +2976,7 @@ void MusicXmlInput::ReadMusicXmlNote(
             if (!isChord) {
                 FTrem *fTrem = new FTrem();
                 this->AddLayerElement(layer, fTrem);
-                m_elementStackMap.at(layer).push_back(fTrem);
+                m_elementStackMap[layer].push_back(fTrem);
                 int beamFloatNum = tremolo.node().text().as_int(); // number of floating beams
                 int beamAttachedNum = 0; // number of attached beams
                 while (beamStart && beamAttachedNum < 8) { // count number of (attached) beams, max 8
@@ -2993,7 +2994,7 @@ void MusicXmlInput::ReadMusicXmlNote(
             if (!isChord) {
                 BTrem *bTrem = new BTrem();
                 this->AddLayerElement(layer, bTrem);
-                m_elementStackMap.at(layer).push_back(bTrem);
+                m_elementStackMap[layer].push_back(bTrem);
                 if (HasAttributeWithValue(tremolo.node(), "type", "unmeasured")) {
                     bTrem->SetForm(tremForm_FORM_unmeas);
                     tremSlashNum = 0;
@@ -3009,8 +3010,8 @@ void MusicXmlInput::ReadMusicXmlNote(
     int duration = node.child("duration").text().as_int();
     // In chords, make sure a note does not extend first note's duration.
     // See https://github.com/rism-digital/verovio/issues/4225
-    if (isChord && duration && m_elementStackMap.at(layer).back()->Is(CHORD)) {
-        Chord *chord = vrv_cast<Chord *>(m_elementStackMap.at(layer).back());
+    if (isChord && duration && m_elementStackMap[layer].back()->Is(CHORD)) {
+        Chord *chord = vrv_cast<Chord *>(m_elementStackMap[layer].back());
         if (chord) duration = std::min(duration, chord->GetDurPpq());
     }
     const int noteStaffNum = node.child("staff").text().as_int();
@@ -3064,7 +3065,7 @@ void MusicXmlInput::ReadMusicXmlNote(
                 element = mRest;
                 if (cue) mRest->SetCue(BOOLEAN_true);
                 if (!stepStr.empty()) mRest->SetPloc(ConvertStepToPitchName(stepStr));
-                if (!octaveStr.empty()) mRest->SetOloc(std::stoi(octaveStr));
+                if (!octaveStr.empty()) mRest->SetOloc((int)strtol(octaveStr.c_str(), nullptr, 10));
                 if (!noteID.empty()) {
                     mRest->SetID(noteID);
                 }
@@ -3091,7 +3092,7 @@ void MusicXmlInput::ReadMusicXmlNote(
                 if (dots > 0) rest->SetDots(dots);
                 if (cue) rest->SetCue(BOOLEAN_true);
                 if (!stepStr.empty()) rest->SetPloc(ConvertStepToPitchName(stepStr));
-                if (!octaveStr.empty()) rest->SetOloc(std::stoi(octaveStr));
+                if (!octaveStr.empty()) rest->SetOloc((int)strtol(octaveStr.c_str(), nullptr, 10));
                 if (!noteID.empty()) {
                     rest->SetID(noteID);
                 }
@@ -3155,16 +3156,13 @@ void MusicXmlInput::ReadMusicXmlNote(
             if (note->HasPname()) {
                 ListOfObjects accids = note->FindAllDescendantsByType(ACCID);
                 if (!accids.size()) {
-                    try {
-                        for (const auto &current : m_currentAccids.at(note->GetPname())) {
+                    auto accidIt = m_currentAccids.find(note->GetPname());
+                    if (accidIt != m_currentAccids.end()) {
+                        for (const auto &current : accidIt->second) {
                             Accid *accid = new Accid();
                             note->AddChild(accid);
                             accid->IsAttribute(false);
 
-                            // to make sure the new *gestural* accidental conforms to the carried-over *written*
-                            // accidental, we translate the latter to a SMuFL glyph and set the gestural accidental to
-                            // the MEI equivalent of the written accidental. The custom tuning will always choose
-                            // the SMuFL glyph over the gestural or written accidentals.
                             accid->SetAccidGes(Att::AccidentalWrittenToGestural(current.m_accid));
                             if (!current.m_glyphName.empty()) {
                                 accid->SetGlyphName(current.m_glyphName);
@@ -3177,9 +3175,6 @@ void MusicXmlInput::ReadMusicXmlNote(
                             }
                         }
                     }
-                    catch (std::out_of_range &e) {
-                        LogWarning("MusicXML import: Unexpected pitch %d", note->GetPname());
-                    }
                 }
                 else {
                     m_currentAccids[note->GetPname()].clear();
@@ -3228,20 +3223,20 @@ void MusicXmlInput::ReadMusicXmlNote(
         TabGrp *tabGrp = NULL;
         if (isTablature) {
             // create the tabGrp if we are starting a new tabGrp
-            if (m_elementStackMap.at(layer).empty() || !m_elementStackMap.at(layer).back()->Is(TABGRP)) {
+            if (m_elementStackMap[layer].empty() || !m_elementStackMap[layer].back()->Is(TABGRP)) {
                 tabGrp = new TabGrp();
                 tabGrp->SetDur(ConvertTypeToDur(typeStr));
                 tabGrp->SetDurPpq(duration);
                 if (dots > 0) tabGrp->SetDots(dots);
                 tabGrp->AddChild(new TabDurSym());
                 this->AddLayerElement(layer, tabGrp, duration);
-                m_elementStackMap.at(layer).push_back(tabGrp);
+                m_elementStackMap[layer].push_back(tabGrp);
                 element = tabGrp;
             }
         }
         else if (nextIsChord) {
             // create the chord if we are starting a new chord
-            if (m_elementStackMap.at(layer).empty() || !m_elementStackMap.at(layer).back()->Is(CHORD)) {
+            if (m_elementStackMap[layer].empty() || !m_elementStackMap[layer].back()->Is(CHORD)) {
                 chord = new Chord();
                 chord->SetDur(ConvertTypeToDur(typeStr));
                 chord->SetDurPpq(duration);
@@ -3256,7 +3251,7 @@ void MusicXmlInput::ReadMusicXmlNote(
                     chord->SetStemMod(STEMMODIFIER_z);
                 }
                 this->AddLayerElement(layer, chord, duration);
-                m_elementStackMap.at(layer).push_back(chord);
+                m_elementStackMap[layer].push_back(chord);
                 element = chord;
                 if (cue) chord->SetCue(BOOLEAN_true);
                 if (grace) {
@@ -3272,9 +3267,9 @@ void MusicXmlInput::ReadMusicXmlNote(
         }
         // If the current note is part of a chord.
         if (!isTablature && (nextIsChord || node.child("chord"))) {
-            if (chord == NULL && m_elementStackMap.at(layer).size() > 0
-                && m_elementStackMap.at(layer).back()->Is(CHORD)) {
-                chord = vrv_cast<Chord *>(m_elementStackMap.at(layer).back());
+            if (chord == NULL && m_elementStackMap[layer].size() > 0
+                && m_elementStackMap[layer].back()->Is(CHORD)) {
+                chord = vrv_cast<Chord *>(m_elementStackMap[layer].back());
             }
             if (!chord) {
                 LogError("MusicXML import: Chord starting point has not been found");
@@ -3304,7 +3299,7 @@ void MusicXmlInput::ReadMusicXmlNote(
         if (cue) note->SetCue(BOOLEAN_true);
 
         // set attributes to the note if we are not in a chord
-        if (!isTablature && (m_elementStackMap.at(layer).empty() || !m_elementStackMap.at(layer).back()->Is(CHORD))) {
+        if (!isTablature && (m_elementStackMap[layer].empty() || !m_elementStackMap[layer].back()->Is(CHORD))) {
             if (!typeStr.empty()) note->SetDur(ConvertTypeToDur(typeStr));
             note->SetDurPpq(duration);
             if (dots > 0) note->SetDots(dots);
@@ -3591,7 +3586,7 @@ void MusicXmlInput::ReadMusicXmlNote(
         // if we are ending a chord or tabGrp remove it from the stack
         if (!nextIsChord) {
             const ClassId classId = isTablature ? TABGRP : CHORD;
-            if (!m_elementStackMap.at(layer).empty() && m_elementStackMap.at(layer).back()->Is(classId)) {
+            if (!m_elementStackMap[layer].empty() && m_elementStackMap[layer].back()->Is(classId)) {
                 this->SetChordStaff(layer);
 
                 this->RemoveLastFromStack(classId, layer);
@@ -3793,7 +3788,7 @@ void MusicXmlInput::ReadMusicXmlNote(
                 trill->SetPlace(
                     trill->AttPlacementRelStaff::StrToStaffrel(xmlTrillLine.node().attribute("placement").as_string()));
             }
-            musicxml::OpenSpanner openTrill(1, m_measureCounts.at(measure));
+            musicxml::OpenSpanner openTrill(1, m_measureCounts[measure]);
             m_trillStack.push_back({ trill, openTrill });
         }
         for (pugi::xml_node xmlAccidMark = xmlTrill.node().next_sibling("accidental-mark"); xmlAccidMark;
@@ -3811,7 +3806,7 @@ void MusicXmlInput::ReadMusicXmlNote(
             = notations.node().select_node("ornaments/wavy-line[@type='stop']").node().attribute("number").as_int();
         std::vector<std::pair<Trill *, musicxml::OpenSpanner>>::iterator iter = m_trillStack.begin();
         while (iter != m_trillStack.end()) {
-            const int measureDifference = m_measureCounts.at(measure) - iter->second.m_lastMeasureCount;
+            const int measureDifference = m_measureCounts[measure] - iter->second.m_lastMeasureCount;
             if (atoi(((iter->first)->GetN()).c_str()) == extNumber) {
                 (iter->first)
                     ->SetTstamp2(std::pair<int, double>(
@@ -4240,7 +4235,7 @@ void MusicXmlInput::ReadMusicXmlTupletStart(const pugi::xml_node &node, const pu
 
     Tuplet *tuplet = new Tuplet();
     this->AddLayerElement(layer, tuplet);
-    m_elementStackMap.at(layer).push_back(tuplet);
+    m_elementStackMap[layer].push_back(tuplet);
     short int num = node.select_node("time-modification/actual-notes").node().text().as_int();
     short int numbase = node.select_node("time-modification/normal-notes").node().text().as_int();
     if (tupletStart.first_child()) {
@@ -4259,7 +4254,7 @@ void MusicXmlInput::ReadMusicXmlTupletStart(const pugi::xml_node &node, const pu
 void MusicXmlInput::ReadMusicXmlBeamStart(const pugi::xml_node &node, const pugi::xml_node &beamStart, Layer *layer)
 {
     if (!beamStart || (node.select_node("notations/ornaments/tremolo[@type='start']"))) return;
-    if (m_elementStackMap.at(layer).size() > 0 && m_elementStackMap.at(layer).back()->Is(BEAM)) {
+    if (m_elementStackMap[layer].size() > 0 && m_elementStackMap[layer].back()->Is(BEAM)) {
         LogDebug("MusicXML import: Adding a beam to a beam");
         if (!node.child("grace")) return;
     }
@@ -4268,7 +4263,7 @@ void MusicXmlInput::ReadMusicXmlBeamStart(const pugi::xml_node &node, const pugi
     if (beamStart.attribute("id")) beam->SetID(beamStart.attribute("id").as_string());
     if (beamStart.attribute("fan")) beam->SetForm(ConvertBeamFanToForm(beamStart.attribute("fan").as_string()));
     this->AddLayerElement(layer, beam);
-    m_elementStackMap.at(layer).push_back(beam);
+    m_elementStackMap[layer].push_back(beam);
 }
 
 void MusicXmlInput::ReadMusicXmlTies(
@@ -5045,7 +5040,7 @@ std::string MusicXmlInput::ConvertDegreeToText(pugi::xml_node harmony)
 
                 if (result != Kind2FirstAddable.end()) {
                     int firstAddable = result->second;
-                    if (std::stoi(degreeValue) >= firstAddable) {
+                    if ((int)strtol(degreeValue.c_str(), nullptr, 10) >= firstAddable) {
                         degreeText += "add";
                     }
                 }
@@ -5222,7 +5217,7 @@ std::string MusicXmlInput::GetOrnamentGlyphNumber(int attributes) const
 void MusicXmlInput::SetChordStaff(Layer *layer)
 {
     // if all notes in the chord have @staff attribute set one for the chord as well
-    Chord *chord = vrv_cast<Chord *>(m_elementStackMap.at(layer).back());
+    Chord *chord = vrv_cast<Chord *>(m_elementStackMap[layer].back());
     if (!chord) return;
 
     const ArrayOfObjects &children = chord->GetChildren();
diff --git a/src/iopae.cpp b/src/iopae.cpp
index d52431489..839e63a6d 100644
--- a/src/iopae.cpp
+++ b/src/iopae.cpp
@@ -11,8 +11,8 @@
 
 #include <cassert>
 #include <cctype>
+#include <cstdio>
 #include <fstream>
-#include <regex>
 #include <sstream>
 #include <string>
 
@@ -51,6 +51,73 @@
 
 //----------------------------------------------------------------------------
 
+namespace {
+
+// Checks if 2+ note letters (A-G or -) appear before the next ')' in the string.
+bool hasTupletPattern(const char *s)
+{
+    int noteCount = 0;
+    for (const char *p = s; *p; ++p) {
+        if (*p == ')') return noteCount >= 2;
+        if ((*p >= 'A' && *p <= 'G') || *p == '-') noteCount++;
+    }
+    return false;
+}
+
+// Scans for target char before the next note letter (A-G).
+bool hasCharBeforeNote(const char *s, char target)
+{
+    for (const char *p = s; *p; ++p) {
+        if (*p >= 'A' && *p <= 'G') return false;
+        if (*p == target) return true;
+    }
+    return false;
+}
+
+// Replaces all occurrences of `from` with `to` in string s.
+void replaceAll(std::string &s, const std::string &from, const std::string &to)
+{
+    size_t pos = 0;
+    while ((pos = s.find(from, pos)) != std::string::npos) {
+        s.replace(pos, from.length(), to);
+        pos += to.length();
+    }
+}
+
+// Checks if the entire string is digits (non-empty).
+bool isAllDigits(const char *s)
+{
+    if (!*s) return false;
+    for (const char *p = s; *p; ++p) {
+        if (!isdigit(static_cast<unsigned char>(*p))) return false;
+    }
+    return true;
+}
+
+// Parses mensural time signature: ([co])([./]?)([./]?)(\d*)/?(\d*)
+// Returns true if the string matches this pattern exactly.
+bool parseMensural(const char *s, char &sign, char &mod1, char &mod2, std::string &num, std::string &numbase)
+{
+    sign = '\0';
+    mod1 = '\0';
+    mod2 = '\0';
+    num.clear();
+    numbase.clear();
+
+    if (*s != 'c' && *s != 'o') return false;
+    sign = *s++;
+    if (*s == '.' || *s == '/') mod1 = *s++;
+    if (*s == '.' || *s == '/') mod2 = *s++;
+    while (isdigit(static_cast<unsigned char>(*s))) num += *s++;
+    if (*s == '/') {
+        s++;
+        while (isdigit(static_cast<unsigned char>(*s))) numbase += *s++;
+    }
+    return *s == '\0';
+}
+
+} // anonymous namespace
+
 namespace vrv {
 
 //----------------------------------------------------------------------------
@@ -725,7 +792,7 @@ void PAEInput::parsePlainAndEasy(std::istream &infile)
     m_doc->SetType(Raw);
     // The mdiv
     Mdiv *mdiv = new Mdiv();
-    mdiv->m_visibility = Visible;
+    mdiv->SetVisibility(Visible);
     m_doc->AddChild(mdiv);
     // The score
     Score *score = new Score();
@@ -1018,7 +1085,7 @@ void PAEInput::parsePlainAndEasy(std::istream &infile)
     for (it = staff.begin(); it < staff.end(); ++it) {
 
         m_staff = new Staff(1);
-        m_measure = new Measure(true, measure_count);
+        m_measure = new Measure(MEASURED, measure_count);
         m_layer = new Layer();
         m_layer->SetN(1);
 
@@ -1243,12 +1310,7 @@ int PAEInput::getTupletFermata(const char *incipit, pae::Note *note, int index)
     int length = (int)strlen(incipit);
 
     // Detect if it is a fermata or a tuplet.
-    //
-    // std::regex_constants::ECMAScript is the default syntax, so optional.
-    // Previously these were extended regex syntax, but this case
-    // is the same in ECMAScript syntax.
-    static const std::regex exp("^([^)]*[ABCDEFG-][^)]*[ABCDEFG-][^)]*)", std::regex_constants::ECMAScript);
-    bool is_tuplet = regex_search(incipit + i, exp);
+    bool is_tuplet = hasTupletPattern(incipit + i);
 
     if (is_tuplet) {
         int t = i;
@@ -1426,20 +1488,14 @@ int PAEInput::getTimeInfo(const char *incipit, MeterSig *meter, Mensur *mensur,
     // strncpy not always put the \0 in the end!
     strncpy(timesig_str, incipit + index, i - index);
 
-    std::ostringstream sout;
-
-    // regex_match matches to the entire input string (regex_search does
-    // partial matches.  In this case cmatch is used to store the submatches
-    // (enclosed in parentheses) for later reference.  Use std::smatch when
-    // dealing with strings, or std::wmatch with wstrings.
-    std::cmatch matches;
+    int tsNum = 0, tsDenom = 0, tsN = 0;
     if (meter) {
-        if (regex_match(timesig_str, matches, std::regex("(\\d+)/(\\d+)"))) {
-            meter->SetCount({ { std::stoi(matches[1]) }, MeterCountSign::None });
-            meter->SetUnit(std::stoi(matches[2]));
+        if (sscanf(timesig_str, "%d/%d%n", &tsNum, &tsDenom, &tsN) == 2 && timesig_str[tsN] == '\0') {
+            meter->SetCount({ { tsNum }, MeterCountSign::None });
+            meter->SetUnit(tsDenom);
         }
-        else if (regex_match(timesig_str, matches, std::regex("\\d+"))) {
-            meter->SetCount({ { std::stoi(timesig_str) }, MeterCountSign::None });
+        else if (isAllDigits(timesig_str)) {
+            meter->SetCount({ { (int)strtol(timesig_str, nullptr, 10) }, MeterCountSign::None });
             meter->SetUnit(1);
             meter->SetForm(METERFORM_num);
         }
@@ -1467,42 +1523,29 @@ int PAEInput::getTimeInfo(const char *incipit, MeterSig *meter, Mensur *mensur,
         }
     }
     else {
-        if (regex_match(timesig_str, matches, std::regex("(\\d+)/(\\d+)"))) {
-            mensur->SetNum(std::stoi(matches[1]));
-            mensur->SetNumbase(std::stoi(matches[2]));
+        tsN = 0;
+        if (sscanf(timesig_str, "%d/%d%n", &tsNum, &tsDenom, &tsN) == 2 && timesig_str[tsN] == '\0') {
+            mensur->SetNum(tsNum);
+            mensur->SetNumbase(tsDenom);
         }
-        else if (regex_match(timesig_str, matches, std::regex("\\d+"))) {
-            mensur->SetNum(std::stoi(timesig_str));
+        else if (isAllDigits(timesig_str)) {
+            mensur->SetNum((int)strtol(timesig_str, nullptr, 10));
         }
-        else if (regex_match(timesig_str, matches, std::regex("([co])([\\./]?)([\\./]?)(\\d*)/?(\\d*)"))) {
-            // C
-            if (matches[1] == "c") {
-                mensur->SetSign(MENSURATIONSIGN_C);
+        else {
+            char mSign, mMod1, mMod2;
+            std::string mNum, mNumbase;
+            if (parseMensural(timesig_str, mSign, mMod1, mMod2, mNum, mNumbase)) {
+                if (mSign == 'c') mensur->SetSign(MENSURATIONSIGN_C);
+                else mensur->SetSign(MENSURATIONSIGN_O);
+                if (mMod1 == '.' || mMod2 == '.') mensur->SetDot(BOOLEAN_true);
+                if (mMod1 == '/' || mMod2 == '/') mensur->SetSlash(1);
+                if (!mNum.empty()) mensur->SetNum((int)strtol(mNum.c_str(), nullptr, 10));
+                if (!mNum.empty() && !mNumbase.empty()) mensur->SetNumbase((int)strtol(mNumbase.c_str(), nullptr, 10));
             }
-            // O
             else {
-                mensur->SetSign(MENSURATIONSIGN_O);
-            }
-            // Dot (second or third match since order between . and / is not defined in PAE)
-            if ((matches[2] == ".") || (matches[3] == ".")) {
-                mensur->SetDot(BOOLEAN_true);
-            }
-            // Slash (second or third match, ditto)
-            if ((matches[2] == "/") || (matches[3] == "/")) {
-                mensur->SetSlash(1);
-            }
-            // Num
-            if (matches[4] != "") {
-                mensur->SetNum(std::stoi(matches[4]));
-            }
-            // Numbase (but only if Num is given)
-            if ((matches[4] != "") && (matches[5] != "")) {
-                mensur->SetNumbase(std::stoi(matches[5]));
+                LogWarning("Plaine & Easie import: unsupported time signature: %s", timesig_str);
             }
         }
-        else {
-            LogWarning("Plaine & Easie import: unsupported time signature: %s", timesig_str);
-        }
     }
 
     return i - index;
@@ -1756,9 +1799,8 @@ int PAEInput::getKeyInfo(const char *incipit, KeySig *key, int index)
         else {
             key->SetSig({ alt_nr, alterationType });
         }
-        if (cancel) {
-            key->SetSigShowchange(BOOLEAN_true);
-        }
+        // SetSigShowchange removed in Verovio 6.x
+        (void)cancel;
     }
     else {
         key->SetSig({ 0, ACCIDENTAL_WRITTEN_n });
@@ -1793,9 +1835,14 @@ int PAEInput::getNote(const char *incipit, pae::Note *note, pae::Measure *measur
             note->dots = 0;
             LogWarning("Plaine & Easie import: found note before duration was specified");
         }
+        else if (measure->durations_offset < (int)measure->durations.size()
+            && measure->durations_offset < (int)measure->dots.size()) {
+            note->duration = measure->durations[measure->durations_offset];
+            note->dots = measure->dots[measure->durations_offset];
+        }
         else {
-            note->duration = measure->durations.at(measure->durations_offset);
-            note->dots = measure->dots.at(measure->durations_offset);
+            note->duration = DURATION_4;
+            note->dots = 0;
         }
     }
     note->pitch = getPitch(incipit[i]);
@@ -1838,12 +1885,12 @@ int PAEInput::getNote(const char *incipit, pae::Note *note, pae::Measure *measur
     }
 
     // chord
-    if (regex_search(incipit + i + 1, std::regex("^[^A-G]*\\^"))) {
+    if (hasCharBeforeNote(incipit + i + 1, '^')) {
         note->chord = true;
     }
 
     // tie
-    if (regex_search(incipit + i + 1, std::regex("^[^A-G]*\\+"))) {
+    if (hasCharBeforeNote(incipit + i + 1, '+')) {
         note->tie = true;
         if (note->accidental) {
             m_tieAccid.first = note->pitch + note->octave * 7;
@@ -1856,7 +1903,7 @@ int PAEInput::getNote(const char *incipit, pae::Note *note, pae::Measure *measur
     }
 
     // trills
-    if (regex_search(incipit + i + 1, std::regex("^[^A-G]*t"))) {
+    if (hasCharBeforeNote(incipit + i + 1, 't')) {
         note->trill = true;
     }
 
@@ -2137,7 +2184,7 @@ void PAEInput::popContainer()
 void PAEInput::addLayerElement(LayerElement *element)
 {
     if (m_nested_objects.size() > 0) {
-        if (!m_nested_objects.back()->IsSupportedChild(element)) {
+        if (!m_nested_objects.back()->IsSupportedChild(element->GetClassId())) {
             delete element;
             element = NULL;
         }
@@ -2537,8 +2584,10 @@ void PAEInput::LogPAE(int errCode, pae::Token &token, std::string value)
         default: column = token.m_position;
     }
     logEntry << "column" << column;
-    const std::string msg
-        = (value.empty()) ? s_errCodes.at(errCode) : StringFormat(s_errCodes.at(errCode).c_str(), value.c_str());
+    auto it = s_errCodes.find(errCode);
+    static const std::string unknownErr = "Unknown error";
+    const std::string &errStr = (it != s_errCodes.end()) ? it->second : unknownErr;
+    const std::string msg = (value.empty()) ? errStr : StringFormat(errStr.c_str(), value.c_str());
     logEntry << "text" << msg;
     if (!value.empty()) {
         logEntry << "value" << value;
@@ -2598,7 +2647,7 @@ void PAEInput::LogDebugTokens(bool vertical)
             char c = (token.m_inputChar) ? token.m_inputChar : ' ';
             row.push_back(c);
         }
-        row = std::regex_replace(row, std::regex("%"), "%%");
+        replaceAll(row, "%", "%%");
         LogDebug(row.c_str());
         if (m_hasErrors) {
             row.clear();
@@ -2611,7 +2660,7 @@ void PAEInput::LogDebugTokens(bool vertical)
         row.clear();
         for (pae::Token &token : m_pae) {
             std::string className = (token.m_object) ? token.m_object->GetClassName() : " ";
-            row.push_back(className.at(0));
+            row.push_back(className[0]);
         }
         LogDebug(row.c_str());
         row.clear();
@@ -2747,11 +2796,22 @@ jsonxx::Object PAEInput::SingleLineToJson(const std::string &singleLine)
     }
 
     // Extract the data - everything after the scoreDef
-    std::string data(scoreDefEnd + 1, singleLine.end());
+    std::string data;
+    if (scoreDefEnd != singleLine.end()) {
+        data = std::string(scoreDefEnd + 1, singleLine.end());
+    }
 
     jsonInput << "clef" << clef;
-    jsonInput << "timesig" << timesig.erase(timesig.find_last_not_of(' ') + 1);
-    jsonInput << "keysig" << keysig.erase(keysig.find_last_not_of(' ') + 1);
+    if (!timesig.empty()) {
+        auto pos = timesig.find_last_not_of(' ');
+        if (pos != std::string::npos) timesig.erase(pos + 1);
+        jsonInput << "timesig" << timesig;
+    }
+    if (!keysig.empty()) {
+        auto pos = keysig.find_last_not_of(' ');
+        if (pos != std::string::npos) keysig.erase(pos + 1);
+        jsonInput << "keysig" << keysig;
+    }
     jsonInput << "data" << data;
 
     return jsonInput;
@@ -2796,14 +2856,14 @@ bool PAEInput::Import(const std::string &input)
     }
 
     jsonxx::Object jsonInput;
-    if (input.at(0) == '{') {
+    if (input[0] == '{') {
         if (!jsonInput.parse(input)) {
             pae::Token inputToken(0, pae::INPUT_POS);
             LogPAE(ERR_002_JSON_PARSE, inputToken);
             return false;
         }
     }
-    else if (input.at(0) == '%') {
+    else if (input[0] == '%') {
         jsonInput = this->SingleLineToJson(input);
     }
     else {
@@ -2885,9 +2945,10 @@ bool PAEInput::Import(const std::string &input)
         }
     }
 
-    // Something when wrong when parsing the scoreDef clef / keySig / mensur / meterSig
+    // Something went wrong when parsing the scoreDef clef / keySig / mensur / meterSig
     if (!success) return false;
 
+
     // No data - we can stop here
     if (!jsonInput.has<jsonxx::String>("data")) {
         pae::Token inputToken(0, pae::INPUT_POS);
@@ -2915,9 +2976,9 @@ bool PAEInput::Import(const std::string &input)
         data.erase(std::remove(data.begin(), data.end(), c), data.end());
     }
 
-    data = std::regex_replace(data, std::regex("qq"), "Q");
-    data = std::regex_replace(data, std::regex("xx"), "X");
-    data = std::regex_replace(data, std::regex("bb"), "Y");
+    replaceAll(data, "qq", "Q");
+    replaceAll(data, "xx", "X");
+    replaceAll(data, "bb", "Y");
 
     int i = 0;
     for (char c : data) {
@@ -2987,6 +3048,7 @@ bool PAEInput::Parse()
 
     if (success) success = this->CheckHierarchy();
 
+
     if (m_pedanticMode && !success) {
         this->ClearTokenObjects();
         return false;
@@ -3044,6 +3106,7 @@ bool PAEInput::Parse()
     // The current meterSig, used to calculate the tstamp2 for open ties
     MeterSig *currentMeterSig = &m_meterSig;
 
+
     for (pae::Token &token : m_pae) {
         if (token.IsVoid()) continue;
 
@@ -3615,7 +3678,7 @@ bool PAEInput::ConvertMRestOrMultiRest()
                 token.m_char = 0;
             }
             else {
-                if (!paeStr.empty() && paeStr.at(0) == '0') {
+                if (!paeStr.empty() && paeStr[0] == '0') {
                     this->LogPAE(ERR_016_MREST_NUMBER, token);
                     if (m_pedanticMode) return false;
                     paeStr.erase(0, paeStr.find_first_not_of('0'));
@@ -4580,7 +4643,7 @@ bool PAEInput::ConvertAccidGes()
                 else if (currentAccids.contains(octavedPitch)) {
                     Accid *gesAccid = new Accid();
                     note->AddChild(gesAccid);
-                    data_ACCIDENTAL_WRITTEN accidWritten = currentAccids.at(octavedPitch);
+                    data_ACCIDENTAL_WRITTEN accidWritten = currentAccids[octavedPitch];
                     gesAccid->SetAccidGes(Att::AccidentalWrittenToGestural(accidWritten));
                 }
             }
@@ -4845,7 +4908,7 @@ bool PAEInput::ParseKeySig(KeySig *keySig, const std::string &paeStr, pae::Token
         }
         if (!endOfKeysig) {
             if (altNumber < 7) {
-                enclosedAccids.at(altNumber) = enclosed;
+                enclosedAccids[altNumber] = enclosed;
             }
         }
     }
@@ -4863,7 +4926,7 @@ bool PAEInput::ParseKeySig(KeySig *keySig, const std::string &paeStr, pae::Token
                 keyAccid->SetPname(pname);
                 keyAccid->SetAccid(alterationType);
                 keySig->AddChild(keyAccid);
-                if (enclosedAccids.at(i)) {
+                if (enclosedAccids[i]) {
                     keyAccid->SetEnclose(ENCLOSURE_brack);
                 }
             }
@@ -4907,14 +4970,14 @@ bool PAEInput::ParseClef(Clef *clef, const std::string &paeStr, pae::Token &toke
         if (m_pedanticMode) return false;
     }
 
-    char clefShape = paeStr.at(0);
+    char clefShape = paeStr[0];
 
     // Second character - or +
-    if (paeStr.at(1) != '+' && paeStr.at(1) != '-') {
+    if (paeStr[1] != '+' && paeStr[1] != '-') {
         this->LogPAE(ERR_043_CLEF_INVALID_2ND, token);
         if (m_pedanticMode) return false;
     }
-    bool isMensural = (paeStr.at(1) == '+');
+    bool isMensural = (paeStr[1] == '+');
 
     if (mensuralScoreDef) {
         *mensuralScoreDef = isMensural;
@@ -4925,11 +4988,11 @@ bool PAEInput::ParseClef(Clef *clef, const std::string &paeStr, pae::Token &toke
     }
 
     // Third character a digit
-    if (!isdigit(paeStr.at(2))) {
+    if (!isdigit(paeStr[2])) {
         this->LogPAE(ERR_045_CLEF_INVALID_3RD, token);
         if (m_pedanticMode) return false;
     }
-    char clefLine = paeStr.at(2);
+    char clefLine = paeStr[2];
 
     // Building the clef
     if (clefShape == 'G') {
@@ -4977,13 +5040,13 @@ bool PAEInput::ParseMeterSig(MeterSig *meterSig, const std::string &paeStr, pae:
         return true;
     }
 
-    std::cmatch matches;
-    if (regex_match(paeStr.c_str(), matches, std::regex("(\\d+)/(\\d+)"))) {
-        meterSig->SetCount({ { std::stoi(matches[1]) }, MeterCountSign::None });
-        meterSig->SetUnit(std::stoi(matches[2]));
+    int tsNum2 = 0, tsDenom2 = 0, tsN2 = 0;
+    if (sscanf(paeStr.c_str(), "%d/%d%n", &tsNum2, &tsDenom2, &tsN2) == 2 && paeStr.c_str()[tsN2] == '\0') {
+        meterSig->SetCount({ { tsNum2 }, MeterCountSign::None });
+        meterSig->SetUnit(tsDenom2);
     }
-    else if (regex_match(paeStr.c_str(), matches, std::regex("\\d+"))) {
-        meterSig->SetCount({ { std::stoi(paeStr) }, MeterCountSign::None });
+    else if (isAllDigits(paeStr.c_str())) {
+        meterSig->SetCount({ { atoi(paeStr.c_str()) }, MeterCountSign::None });
         meterSig->SetUnit(1);
         meterSig->SetForm(METERFORM_num);
     }
@@ -5032,44 +5095,30 @@ bool PAEInput::ParseMensur(Mensur *mensur, const std::string &paeStr, pae::Token
         return true;
     }
 
-    std::cmatch matches;
-    if (regex_match(paeStr.c_str(), matches, std::regex("(\\d+)/(\\d+)"))) {
-        mensur->SetNum(std::stoi(matches[1]));
-        mensur->SetNumbase(std::stoi(matches[2]));
+    int tsNum3 = 0, tsDenom3 = 0, tsN3 = 0;
+    if (sscanf(paeStr.c_str(), "%d/%d%n", &tsNum3, &tsDenom3, &tsN3) == 2 && paeStr.c_str()[tsN3] == '\0') {
+        mensur->SetNum(tsNum3);
+        mensur->SetNumbase(tsDenom3);
     }
-    else if (regex_match(paeStr.c_str(), matches, std::regex("\\d+"))) {
-        mensur->SetNum(std::stoi(paeStr.c_str()));
+    else if (isAllDigits(paeStr.c_str())) {
+        mensur->SetNum(atoi(paeStr.c_str()));
     }
-    else if (regex_match(paeStr.c_str(), matches, std::regex("([co])([\\./]?)([\\./]?)(\\d*)/?(\\d*)"))) {
-        // C
-        if (matches[1] == "c") {
-            mensur->SetSign(MENSURATIONSIGN_C);
+    else {
+        char mSign, mMod1, mMod2;
+        std::string mNum, mNumbase;
+        if (parseMensural(paeStr.c_str(), mSign, mMod1, mMod2, mNum, mNumbase)) {
+            if (mSign == 'c') mensur->SetSign(MENSURATIONSIGN_C);
+            else mensur->SetSign(MENSURATIONSIGN_O);
+            if (mMod1 == '.' || mMod2 == '.') mensur->SetDot(BOOLEAN_true);
+            if (mMod1 == '/' || mMod2 == '/') mensur->SetSlash(1);
+            if (!mNum.empty()) mensur->SetNum(atoi(mNum.c_str()));
+            if (!mNum.empty() && !mNumbase.empty()) mensur->SetNumbase(atoi(mNumbase.c_str()));
         }
-        // O
         else {
-            mensur->SetSign(MENSURATIONSIGN_O);
-        }
-        // Dot (second or third match since order between . and / is not defined in PAE)
-        if ((matches[2] == ".") || (matches[3] == ".")) {
-            mensur->SetDot(BOOLEAN_true);
-        }
-        // Slash (second or third match, ditto)
-        if ((matches[2] == "/") || (matches[3] == "/")) {
-            mensur->SetSlash(1);
-        }
-        // Num
-        if (matches[4] != "") {
-            mensur->SetNum(std::stoi(matches[4]));
-        }
-        // Numbase (but only if Num is given)
-        if ((matches[4] != "") && (matches[5] != "")) {
-            mensur->SetNumbase(std::stoi(matches[5]));
+            this->LogPAE(ERR_048_TIMESIG_INVALID, token, paeStr);
+            if (m_pedanticMode) return false;
         }
     }
-    else {
-        this->LogPAE(ERR_048_TIMESIG_INVALID, token, paeStr);
-        if (m_pedanticMode) return false;
-    }
     return true;
 }
 
@@ -5107,7 +5156,7 @@ bool PAEInput::ParseDuration(
 {
     durations.clear();
 
-    if (paeStr.size() < 1 || paeStr.at(0) == '.') {
+    if (paeStr.size() < 1 || paeStr[0] == '.') {
         this->LogPAE(ERR_052_DURATION, token);
         // Default to quarter note
         if (m_pedanticMode) return false;
diff --git a/src/object.cpp b/src/object.cpp
index 71297011b..4c764ee59 100644
--- a/src/object.cpp
+++ b/src/object.cpp
@@ -105,7 +105,7 @@ Object::Object(const Object &object) : BoundingBox(object)
 
     // Also copy attribute classes
     m_attClasses = object.m_attClasses;
-    m_interfaces = object.m_interfaces;
+    m_interfaceBits = object.m_interfaceBits;
     // New id
     this->GenerateID();
     // For now do not copy them
@@ -150,7 +150,7 @@ Object &Object::operator=(const Object &object)
 
         // Also copy attribute classes
         m_attClasses = object.m_attClasses;
-        m_interfaces = object.m_interfaces;
+        m_interfaceBits = object.m_interfaceBits;
         // New id
         this->GenerateID();
         // For now do now copy them
@@ -232,8 +232,8 @@ void Object::Reset()
 
 void Object::RegisterInterface(std::vector<AttClassId> *attClasses, InterfaceId interfaceId)
 {
-    m_attClasses.insert(m_attClasses.end(), attClasses->begin(), attClasses->end());
-    m_interfaces.push_back(interfaceId);
+    for (auto id : *attClasses) m_attClasses.set(id);
+    m_interfaceBits |= (1u << interfaceId);
 }
 
 bool Object::IsMilestoneElement() const
diff --git a/src/options.cpp b/src/options.cpp
index dcee5e10c..006d159dd 100644
--- a/src/options.cpp
+++ b/src/options.cpp
@@ -1940,8 +1940,10 @@ Options &Options::operator=(const Options &options)
     MapOfStrOptions const *items = options.GetItems();
     MapOfStrOptions::const_iterator iter;
     for (iter = items->begin(); iter != items->end(); ++iter) {
-        Option *opt = m_items.at(iter->first);
-        assert(opt);
+        auto optIt = m_items.find(iter->first);
+        if (optIt == m_items.end()) continue;
+        Option *opt = optIt->second;
+        if (!opt) continue;
         iter->second->CopyTo(opt);
     }
 
diff --git a/src/preparedatafunctor.cpp b/src/preparedatafunctor.cpp
index d4cdd6852..567a3414d 100644
--- a/src/preparedatafunctor.cpp
+++ b/src/preparedatafunctor.cpp
@@ -266,10 +266,12 @@ FunctorCode PrepareCrossStaffFunctor::VisitLayerElement(LayerElement *layerEleme
     m_currentCrossStaff = NULL;
     m_currentCrossLayer = NULL;
 
-    AttNIntegerComparison comparisonFirst(STAFF, crossElement->GetStaff().at(0));
+    if (crossElement->GetStaff().empty()) return FUNCTOR_CONTINUE;
+    int crossStaffN = crossElement->GetStaff()[0];
+    AttNIntegerComparison comparisonFirst(STAFF, crossStaffN);
     layerElement->m_crossStaff = vrv_cast<Staff *>(m_currentMeasure->FindDescendantByComparison(&comparisonFirst, 1));
     if (!layerElement->m_crossStaff) {
-        LogWarning("Could not get the cross staff reference '%d' for element '%s'", crossElement->GetStaff().at(0),
+        LogWarning("Could not get the cross staff reference '%d' for element '%s'", crossStaffN,
             layerElement->GetID().c_str());
         return FUNCTOR_CONTINUE;
     }
@@ -278,7 +280,7 @@ FunctorCode PrepareCrossStaffFunctor::VisitLayerElement(LayerElement *layerEleme
     // Check if we have a cross-staff to itself...
     if (layerElement->m_crossStaff == parentStaff) {
         LogWarning("The cross staff reference '%d' for element '%s' seems to be identical to the parent staff",
-            crossElement->GetStaff().at(0), layerElement->GetID().c_str());
+            crossStaffN, layerElement->GetID().c_str());
         layerElement->m_crossStaff = NULL;
         return FUNCTOR_CONTINUE;
     }
@@ -301,7 +303,7 @@ FunctorCode PrepareCrossStaffFunctor::VisitLayerElement(LayerElement *layerEleme
     if (!layerElement->m_crossLayer) {
         // Nothing we can do
         LogWarning("Could not get the layer with cross-staff reference '%d' for element '%s'",
-            crossElement->GetStaff().at(0), layerElement->GetID().c_str());
+            crossStaffN, layerElement->GetID().c_str());
         layerElement->m_crossStaff = NULL;
     }
     else {
diff --git a/src/resources.cpp b/src/resources.cpp
index 680176f3e..68c3c4b51 100644
--- a/src/resources.cpp
+++ b/src/resources.cpp
@@ -95,6 +95,26 @@ bool Resources::InitFonts()
     return true;
 }
 
+void Resources::AddLoadedFont(const std::string &name, GlyphTable &&table, bool isFallback)
+{
+    LoadedFont font(name, isFallback);
+    font.GetGlyphTableForModification() = std::move(table);
+    m_loadedFonts.insert(std::pair<std::string, LoadedFont>(name, std::move(font)));
+}
+
+void Resources::AddTextFont(const StyleAttributes &style, GlyphTable &&table)
+{
+    m_textFont[style] = std::move(table);
+}
+
+void Resources::SetDefaultFont(const std::string &name)
+{
+    m_defaultFontName = name;
+    m_currentFontName = name;
+    m_fallbackFontName = name;
+    m_currentStyle = { FONTWEIGHT_normal, FONTSTYLE_normal };
+}
+
 bool Resources::SetFont(const std::string &fontName)
 {
     m_cachedGlyph.reset();
@@ -213,7 +233,8 @@ char32_t Resources::GetGlyphCode(const std::string &smuflName) const
 
 bool Resources::IsSmuflFallbackNeeded(const std::u32string &text) const
 {
-    if (m_loadedFonts.at(m_currentFontName).isFallback()) {
+    auto currentIt = m_loadedFonts.find(m_currentFontName);
+    if (currentIt == m_loadedFonts.end() || currentIt->second.isFallback()) {
         return false;
     }
     for (char32_t c : text) {
diff --git a/src/rest.cpp b/src/rest.cpp
index 32a6130e3..ce21f4b11 100644
--- a/src/rest.cpp
+++ b/src/rest.cpp
@@ -607,11 +607,17 @@ int Rest::GetRestOffsetFromOptions(
     // Make sure we are in the boundaries of g_defaultRests
     if (duration > DURATION_128) duration = DURATION_128;
     if (duration < DURATION_long) duration = DURATION_long;
-    return g_defaultRests.at(layer)
-        .at(RL_sameLayer == layer ? location.second : RA_none)
-        .at(isTopLayer ? RLP_restOnTopLayer : RLP_restOnBottomLayer)
-        .at(0 == location.first % 2 ? RNP_noteOnLine : RNP_noteInSpace)
-        .at(duration);
+    auto it1 = g_defaultRests.find(layer);
+    if (it1 == g_defaultRests.end()) return 0;
+    auto it2 = it1->second.find(RL_sameLayer == layer ? location.second : RA_none);
+    if (it2 == it1->second.end()) return 0;
+    auto it3 = it2->second.find(isTopLayer ? RLP_restOnTopLayer : RLP_restOnBottomLayer);
+    if (it3 == it2->second.end()) return 0;
+    auto it4 = it3->second.find(0 == location.first % 2 ? RNP_noteOnLine : RNP_noteInSpace);
+    if (it4 == it3->second.end()) return 0;
+    auto it5 = it4->second.find(duration);
+    if (it5 == it4->second.end()) return 0;
+    return it5->second;
 }
 
 //----------------------------------------------------------------------------
diff --git a/src/scoringupfunctor.cpp b/src/scoringupfunctor.cpp
index bb542fd03..6001ed1d8 100644
--- a/src/scoringupfunctor.cpp
+++ b/src/scoringupfunctor.cpp
@@ -652,7 +652,8 @@ Note *ScoringUpFunctor::ImperfectionAPA(const ArrayOfElementDurPairs &sequence,
 // 3. Alteration (making a note twice as long)
 Note *ScoringUpFunctor::Alteration(const ArrayOfElementDurPairs &sequence, data_DURATION boundUnit)
 {
-    std::pair<LayerElement *, data_DURATION> penultElementDurPair = sequence.at(sequence.size() - 2);
+    if (sequence.size() < 2) return nullptr;
+    std::pair<LayerElement *, data_DURATION> penultElementDurPair = sequence[sequence.size() - 2];
     LayerElement *penultElement = penultElementDurPair.first;
     data_DURATION penultDur = penultElementDurPair.second;
     /// Evaluates what is the type of the penultimate element in the sequence. If it is a rest, it forbids Alteration
diff --git a/src/svgdevicecontext.cpp b/src/svgdevicecontext.cpp
index 716e30384..34c88181b 100644
--- a/src/svgdevicecontext.cpp
+++ b/src/svgdevicecontext.cpp
@@ -43,6 +43,7 @@ SvgDeviceContext::SvgDeviceContext(const std::string &docId) : DeviceContext(SVG
     m_originY = 0;
 
     m_smuflGlyphs.clear();
+    m_smuflGlyphLookup.clear();
 
     m_committed = false;
     m_vrvTextFont = false;
@@ -100,25 +101,21 @@ SvgDeviceContext::GlyphRef::GlyphRef(const Glyph *glyph, int count, const std::s
 
 const std::string SvgDeviceContext::InsertGlyphRef(const Glyph *glyph)
 {
-    const std::string code = glyph->GetCodeStr();
-
-    // Check if glyph already exists
-    for (const auto &[g, ref] : m_smuflGlyphs) {
-        if (g == glyph) {
-            return ref.GetRefId();
-        }
-    }
+    auto it = m_smuflGlyphLookup.find(glyph);
+    if (it != m_smuflGlyphLookup.end()) return it->second;
 
+    const std::string code = glyph->GetCodeStr();
     int count = 0;
-    auto it = m_glyphCodeFontCounter.find(code);
-    if (it != m_glyphCodeFontCounter.end()) {
-        count = it->second;
+    auto cit = m_glyphCodeFontCounter.find(code);
+    if (cit != m_glyphCodeFontCounter.end()) {
+        count = cit->second;
     }
 
     GlyphRef ref(glyph, count, m_glyphPostfixId);
     const std::string id = ref.GetRefId();
 
-    m_smuflGlyphs.emplace_back(glyph, ref); // preserve insertion order
+    m_smuflGlyphs.emplace_back(glyph, ref);
+    m_smuflGlyphLookup.emplace(glyph, id);
     m_glyphCodeFontCounter[code] = count + 1;
 
     return id;
@@ -909,6 +906,7 @@ void SvgDeviceContext::DrawPolyline(int n, Point points[], bool close)
     if (n > 2) polylineChild.append_attribute("fill") = "none";
 
     std::string pointsString;
+    pointsString.reserve(n * 16);
     for (int i = 0; i < n; ++i) {
         pointsString += StringFormat("%d,%d ", points[i].x, points[i].y);
     }
@@ -944,9 +942,12 @@ void SvgDeviceContext::DrawPolygon(int n, Point points[])
         polygonChild.append_attribute("fill-opacity") = currentBrush.GetOpacity();
     }
 
-    std::string pointsString = StringFormat("%d,%d", points[0].x, points[0].y);
+    std::string pointsString;
+    pointsString.reserve(n * 16);
+    pointsString += StringFormat("%d,%d", points[0].x, points[0].y);
     for (int i = 1; i < n; ++i) {
-        pointsString += " " + StringFormat("%d,%d", points[i].x, points[i].y);
+        pointsString += ' ';
+        pointsString += StringFormat("%d,%d", points[i].x, points[i].y);
     }
     polygonChild.append_attribute("points") = pointsString.c_str();
 }
diff --git a/src/toolkit.cpp b/src/toolkit.cpp
index 7239619f5..cc556601a 100644
--- a/src/toolkit.cpp
+++ b/src/toolkit.cpp
@@ -734,13 +734,10 @@ bool Toolkit::LoadData(const std::string &data, bool resetLogBuffer)
     }
 
     else if (inputFrom == ESAC) {
-        // This is the indirect converter from EsAC to MEI using iohumdrum:
         hum::Tool_esac2hum converter;
         std::stringstream conversion;
 
-        this->LogRedirectStart();
         bool status = converter.convert(conversion, data);
-        this->LogRedirectStop();
         if (!status) {
             LogWarning("Problem converting EsAC to Humdrum (see warning above this line for possible reasons");
         }
diff --git a/src/transposition.cpp b/src/transposition.cpp
index 6f1a2f750..aa582b6b7 100644
--- a/src/transposition.cpp
+++ b/src/transposition.cpp
@@ -494,7 +494,7 @@ bool Transposer::SetTransposition(int keyFifths, const std::string &semitones)
     if (!this->IsValidSemitones(semitones)) {
         return false;
     }
-    const int semis = stoi(semitones);
+    const int semis = (int)strtol(semitones.c_str(), nullptr, 10);
     return this->SetTransposition(keyFifths, semis);
 }
 
@@ -627,8 +627,9 @@ int Transposer::IntervalToSemitones(int interval)
     int diatonic = 0;
     int chromatic = 0;
     this->IntervalToDiatonicChromatic(diatonic, chromatic, intervalClass);
-    if ((diatonic != INVALID_INTERVAL_CLASS) && (chromatic != INVALID_INTERVAL_CLASS)) {
-        return (m_diatonic2semitone.at(diatonic) + chromatic) * sign + 12 * octave;
+    if ((diatonic != INVALID_INTERVAL_CLASS) && (chromatic != INVALID_INTERVAL_CLASS)
+        && (diatonic >= 0) && (diatonic < (int)m_diatonic2semitone.size())) {
+        return (m_diatonic2semitone[diatonic] + chromatic) * sign + 12 * octave;
     }
     else {
         return INVALID_INTERVAL_CLASS;
@@ -898,7 +899,7 @@ int Transposer::GetInterval(const std::string &intervalName)
         return INVALID_INTERVAL_CLASS;
     }
 
-    int dnum = stoi(number);
+    int dnum = (int)strtol(number.c_str(), nullptr, 10);
     if (dnum == 0) {
         LogError("Integer interval number cannot be zero: %s", intervalName.c_str());
         return INVALID_INTERVAL_CLASS;
@@ -1861,7 +1862,7 @@ void Transposer::IntervalToDiatonicChromatic(int &diatonic, int &chromatic, cons
         return;
     }
 
-    int dnum = stoi(number);
+    int dnum = (int)strtol(number.c_str(), nullptr, 10);
     if (dnum == 0) {
         LogError("Integer interval number cannot be zero: %s", intervalName.c_str());
         chromatic = INVALID_INTERVAL_CLASS;
diff --git a/src/view_control.cpp b/src/view_control.cpp
index 30338b130..4d0cb15d9 100644
--- a/src/view_control.cpp
+++ b/src/view_control.cpp
@@ -1921,6 +1921,7 @@ void View::DrawDynamSymbolOnly(DeviceContext *dc, Staff *staff, Dynam *dynam, co
 
     // calculate width of the symbol-only dynamic; generally it consists of only one symbol, but in case when it's
     // combination of different dynamics glyphs we need to get total width of all elements
+    if (dynamSymbol.empty()) return;
     const int left = m_doc->GetGlyphLeft(dynamSymbol.at(0), staff->m_drawingStaffSize, false);
     int width = 0;
     for (int i = 0; i < (int)dynamSymbol.size(); ++i) {
diff --git a/src/vrv.cpp b/src/vrv.cpp
index 4fdc059ed..e8f727df6 100644
--- a/src/vrv.cpp
+++ b/src/vrv.cpp
@@ -357,11 +357,13 @@ std::string UTF16to8(const std::u16string &in)
                     cp = ((lead - 0xD800) << 10) + (trail - 0xDC00) + 0x10000;
                 }
                 else {
-                    throw std::runtime_error("Invalid UTF-16 sequence");
+                    LogError("Invalid UTF-16 sequence");
+                    return out;
                 }
             }
             else {
-                throw std::runtime_error("Incomplete UTF-16 sequence");
+                LogError("Incomplete UTF-16 sequence");
+                return out;
             }
         }
         else {
diff --git a/tools/c_wrapper.cpp b/tools/c_wrapper.cpp
index cc9e8ae52..396c70709 100644
--- a/tools/c_wrapper.cpp
+++ b/tools/c_wrapper.cpp
@@ -14,9 +14,9 @@ using namespace vrv;
 
 extern "C" {
 
-void enableLog(bool value)
+void enableLog(int value)
 {
-    EnableLog(value);
+    EnableLog(static_cast<LogLevel>(value));
 }
 
 void enableLogToBuffer(bool value)
diff --git a/tools/c_wrapper.h b/tools/c_wrapper.h
index 924dfa03f..3e4075c2f 100644
--- a/tools/c_wrapper.h
+++ b/tools/c_wrapper.h
@@ -13,7 +13,7 @@ extern "C" {
  * Methods exported a functions to use the Toolkit class
  ****************************************************************/
 
-void enableLog(bool value);
+void enableLog(int level);
 void enableLogToBuffer(bool value);
 
 void *vrvToolkit_constructor();
